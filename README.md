<img src="icon.png" width="88" align="right" alt="音蔵">

# 音蔵 — Otogura

**[English version / 英語版はこちら](README.en.md)**

Windows 用の音楽プレイヤー（Electron）。
ジャンル → アーティスト → アルバムの 3 カラムで絞り込み、下の一覧から再生します。

**音をしまう蔵。** 名前のとおり、**大きなライブラリのために作りました**。
作者の **171,085 曲 / 387 GB** で実際に測って直しています。

**このアプリは、AI に作らせた見本です。** 経緯は下の「どう作ったか」に書いています。

---

## 使う

### いちばん簡単な方法 — 出来上がったものを落とす

[Releases](https://github.com/xmichinokux/otogura/releases) から、どちらかを落としてください。
**Node.js は要りません。**

| 落とすもの | どんなとき |
|---|---|
| `Otogura-Setup-0.43.1.exe` | **ふつうはこちら。** インストールしてスタートメニューに入ります |
| `Otogura-Portable-0.43.1.exe` | インストールしたくないとき。1 個で動きます（USB でも可） |

★**入れた直後の数回は、立ち上がりに時間がかかることがあります。**
署名を買っていないので、Windows が 190 MB の実行ファイルを初回に検査します。
**壊れていません。待てば必ず立ち上がります。**数回で収まります。
（実測: 入れ直した直後に 2 回、その後 1 回 固まり、それ以降は出なくなりました）

---

<details>
<summary>★「Windows によって PC が保護されました」と出たときは</summary>

**署名を買っていないため、Windows がこの警告を出します。**
配布元が誰か確認できない、という意味です。危険だと判定されたわけではありません。

開くには「**詳細情報**」→「**実行**」。

心配なら、次のどれかをおすすめします。

- **中身を自分で確かめる。** ソースは全部ここにあります。下の「ソースから動かす」で同じものが動きます
- ファイルを [VirusTotal](https://www.virustotal.com/) に上げて調べる

★署名（コードサイニング証明書）は年間で数万円かかります。
**個人の見本アプリなので、いまは買っていません。** 隠さずに書いておきます。

**落としたものが本物か、自分で確かめられます。**
PowerShell で次を実行し、下の値と一致するか見てください。

```
Get-FileHash Otogura-Setup-0.43.1.exe -Algorithm SHA256
```

| ファイル | SHA256 |
|---|---|
| `Otogura-Setup-0.43.1.exe` | `5540a4b6cf37e2c4230ee8f4376e85bd89bedd86aa37e291814a4bd364ee3dd7` |
| `Otogura-Portable-0.43.1.exe` | `def569b7dacb877db4b4c070fdb3d2890df9c1db53c60d29823b7cbf55490896` |

★これは**途中で書き換えられていないこと**の確認です。
「作った人が信用できるか」は別の話なので、そこは中身を読んで判断してください。

</details>

---

### ソースから動かす

#### 1. 用意する

[Node.js](https://nodejs.org/) が要ります（18 以上）。

```
git clone https://github.com/xmichinokux/otogura.git
cd otogura
npm install
```

#### 2. デスクトップにアイコンを作る（おすすめ）

**`デスクトップにアイコンを作る.bat` を一度ダブルクリック**してください。

デスクトップに「音蔵」のアイコンができます。
**そのアイコンから開けば、黒い窓は一瞬も出ません。**

<img src="icon.png" width="72" alt="アイコン">

スタートメニューに置きたいときは、できたアイコンを右クリック →
「スタートメニューにピン留めする」。

★アイコンの絵は `tools/make-icon.js` が作っています。
画像ファイルを直に置かず、**作り直せる形**にしてあります
（色や形を変えたいときは、そこを直して `node tools/make-icon.js`）。

#### 2'. アイコンを作らずに起動する

**`起動.bat` をダブルクリック**でも開きます（黒い窓が一瞬出ます）。

コマンドで動かす場合はこちら。

```
npm start
```

★どれも中身は同じで、`electron.exe` を直に呼んでいます。
`npm start` を経由すると間に窓がもう 1 枚挟まり、
**それを閉じたときにアプリごと落ちます。**

---

## 3. 音楽を登録する

下端の**「フォルダを追加」**から、音楽の入ったフォルダを選びます。

拾うのは **`.mp3` `.m4a` `.wav` `.flac`** です。
CD からの取り込み機は、MP3 のつもりでも `.m4a` で書き出すものがあります
（本人の機械がそうでした。詳しくは `src/library.js` の `拾う拡張子`）。

★**aiff と m4p は拾いません。** 実測で鳴らなかったからです
（m4p は iTunes の DRM 付き）。**鳴ることを確かめた形式だけ**を並べています。

★**wav はタグをほとんど持ちません**（手元の 1,268 件中、タグがあったのは 44 件）。
足すと「タグなし」が一気に増えますが、既定の「タグ無しを隠す」が入っていれば見えません。

★**100 MB を超える曲は、音量そろえを測りません。**
測定はファイル全体をメモリに載せるので、大きい wav では持ちません
（実測: 1,095 MB の wav でピーク約 2.1 GB・18 秒）。
測らないだけで**再生はできます**。そのときは下端にそう出ます。

★**`.m4a` のうち ALAC（Apple ロスレス）は鳴りません。**
Chromium が対応していないためです（実測: `DEMUXER_ERROR_NO_SUPPORTED_STREAMS`）。

**一覧からは消しません。** 出したうえで、押したときに理由を出します。
持っている曲が黙って消えるほうが困るからです。
自動送りと ⏭ では飛ばしますが、**飛ばした数を画面に出します**。

設定と読み取り結果は、次の場所に保存されます（**音楽ファイルは動かしません**）。

```
C:\Users\（自分）\AppData\Roaming\Otogura\
  settings.json         フォルダ・再生リスト・音量など
  library-cache.json    読み取ったタグの覚え書き（2 回目以降が速くなる）
```

★このフォルダ名は**アプリ名から決まります**（Electron の決まり）。
名前を変えると保存先も変わるので、**古い名前で使っていたぶんは初回に引き継ぎます**。
写すだけなので、古いフォルダはそのまま残ります。

---

## できること

| | |
|---|---|
| 絞り込み | 3 カラム。**タブで「ジャンル/アーティスト/アルバム」「日付（年/月/日）」「🌐 響き」を切り替え**。Ctrl / Shift で複数選べます |
| 再生 | 一覧を **1 回押す**だけ。**▶ を押すだけでも流れ始めます**（曲を選ばなくていい） |
| シャッフル | **同じアーティスト・アルバムが続きません**。**再生回数の少ない曲を選びやすい**（忘れている曲が出てくる）。対象は**下の一覧に出ているぶん**。曲ごとに**くじから外せます** |
| 音量 | 全体の音量つまみ ＋ **曲ごとの音量差をならす**（ReplayGain と同じ考え方） |
| 並べ替え | 見出しを押すと 曲名・アーティスト・アルバム・**日付**・再生回数。列の幅も変えられる |
| 並び順の既定 | **アーティスト順**（同じ人の中は アルバム → 曲番号 の順） |
| 再生リスト | 作成・並べ替え・m3u の書き出しと読み込み |
| タグの編集 | ジャンル・アーティスト・アルバムを直せる（**MP3 のみ**） |
| タグの無い曲 | **隠す／出す**を切り替え（書き出したままの音源を出さないため） |
| 言葉から辿る | **任意。** 言葉を 1 つ入れると、そこから辿れる音楽を探し、手元にあるものを教えます（API キーが要ります） |
| APIキーを入れる | **任意・既定は切。** 入れると、気分を書いて AI に一本を組ませられます |
| 一覧から外す | 外せて、**戻せます**。外したものがあると「↩ 外した N 曲を戻す」が出ます |

### 同じアーティスト・アルバムを続けない

シャッフルは、**直前と同じアーティスト（と同じアルバム）を避けて**選びます。

★**どこで効くか測ってから入れました。** 3,000 回引いて、直前と同じだった割合です。

| 範囲 | 同じアーティスト | 同じアルバム |
|---|---|---|
| 全部 86,044 曲 | 0.1% → 0.0% | 0.0% → 0.0% |
| ジャンル 1 つ 6,048 曲 | 1.5% → 0.0% | 0.7% → 0.0% |
| アルバム 10 枚 101 曲 | 9.3% → 0.2% | 9.3% → 0.2% |
| **アルバム 5 枚 54 曲** | **28.1% → 4.7%** | 19.9% → 1.4% |
| **アルバム 3 枚 35 曲** | **32.5% → 1.6%** | 32.5% → 1.6% |
| **アーティスト 3 人 180 曲** | **36.5% → 0.9%** | 12.2% → 0.2% |

**広いところでは、もともと起きていませんでした。** 効くのは 3 カラムで数枚だけ選んだときです。

★**見るのは直前の 1 曲だけ**にしてあります。窓を広げるほど「くじ」ではなくなるので、
「連続しない」の一番弱い形にとどめました。

★**避けられないときは避けません。** アルバムを 1 枚だけ選べば、候補は全部同じ
アーティスト・同じアルバムです。そこで止めたら、選んだとおりに流れません。
段階的にゆるめます（別のアーティスト → せめて別のアルバム → 避けない）。

★**締め出しません。** 上の測定で、どの範囲でも**全曲が出る**ことを確かめています。

---

### 一本を組む（任意 ／ 既定は切）

下端の「**🔑 APIキーを入れる**」で API キーを入れると、3 カラムの上に欄が出ます。

```
🎧 [いまの気分を書く]  [一本を組む]     🌱 [言葉を 1 つ入れて辿る]  [辿る]
対象の幅 [──●──] ふつう   選出の量 [──●──] 30 曲
```

**入り口は 2 つあります。** 押す前に、どちらのやり方か決まっています。

| 入り口 | どうなるか |
|---|---|
| 🎧 **気分から** | 書いた気分に合うジャンルに絞り、その中から**一本を組みます** |
| 🌱 **言葉から辿る** | その言葉から辿れる音楽を探し、**手元にあったものを「🌐 響き」タブに集めます** |

★**一本を組むのは 🎧 だけです。** 🌱 は絞り込むところまでで、
そのあとは ▶ か 🔀 で流します（辿った結果そのものが、聴く範囲になります）。

#### つまみ ―― 対象の幅と、選出の量

つまみは **1 組だけ**で、**両方の入り口に効きます**。
2 つの道は、最後は同じところ（一本を組ませる）に出るので、意味が共通になります。

| つまみ | 何が変わるか |
|---|---|
| **対象の幅** | AI に渡す候補の曲数（両方）／ 辿る名前の数（🌱）／ 選ばせるジャンル数（🎧） |
| **選出の量** | 一本を何曲にするか（🎧 だけ。🌱 は一本を組まないので効きません） |

| 目盛 | 候補 | 辿る名前 | ジャンル | 曲数 | 🎧 のお金 | 🌱 のお金 |
|---|---|---|---|---|---|---|
| 1 狭い | 50 曲 | 10 個 | 2 まで | 10 曲 | 約 3.5 円 | 約 6 円 |
| 2 | 100 曲 | 20 個 | 3 まで | 20 曲 | 約 6 円 | 約 8 円 |
| **3 ふつう** | **200 曲** | **35 個** | **5 まで** | **30 曲** | **約 8.3 円** | **約 10 円** |
| 4 | 400 曲 | 55 個 | 8 まで | 50 曲 | 約 12 円 | 約 13 円 |
| 5 広い | 800 曲 | 90 個 | 12 まで | 80 曲 | 約 24 円 | 約 18 円 |

★🌱 は 0.18.0 で高くなりました。**出るバンドを増やすため**に、
AI に見せる演者を 150 組 → 800 組、辿る名前を 21 → 35 個にしたためです。
（それでも一本を組まないので、通信は 1 往復です）
「候補」と「曲数」は 🎧 にだけかかります。

★**真ん中が、これまでの値です。** つまみを触らなければ動きは変わりません。

★**上を 800 曲で止めたのは、費用ではなく効きが鈍るからです。**
候補を増やすほど良い一本になるわけではなく、AI が見渡す量が増えるだけで、
1 曲あたりの吟味は薄くなります。

★**幅を狭く・量を多くすると、候補が足りなくなります。**
（候補 50 曲から 80 曲は取れません）そのときは候補の数まで減らし、
**減らしたことを画面に出します。** 黙って短い一本にはしません。

★お金は**目安**です。曲名やアーティスト名の長さで少し前後しますし、
料金も為替も変わります（Opus 5 の 100 万トークンあたり $5 / $25 を 150 円/ドルで換算）。

★**一本ができても、勝手には流れません。** 再生リストができるだけです。
聴くときは ▶ を押してください。

### AI は曲を選びません。絞り込みを選びます

★**0.35.0 から、ジャンルだけでなく「演者」も選べます。**
ジャンル名では言い表せないもの（音の質感、時代、界隈、「〜みたいな」）は、
演者のほうが正確に伝わるからです。詳しくは下の 0.35.0 の項を見てください。

```
書いた言葉
   ↓
3 カラムがその状態になる → 下の一覧が絞られる → そこから一本を組む
```

そうしている理由が 3 つあります。**何をしたか画面に見える**（納得できなければ、
そのまま手で直せます）。**無い曲を作られない**（返ってきた名前は、実在するか
アプリ側で照合します）。そして**そのあとは、いつものシャッフルが働きます**。

#### ★候補は、組を絞って 1 組から何曲か引きます

AI には候補 200 曲を渡します。**その 200 曲の選び方が効きます。**

曲を 1 曲ずつ引くと、持っている曲数が多いバンドほど何度も当たります。
すると AI は偏った 200 曲から選ぶことになり、頼み文に「同じアーティストを
続けない」と書いても、**1 曲おきに同じバンドが並んで決まりは守られてしまいます**。

演者を 1 周して 1 曲ずつ取り、足りなければ 2 周目に入ります。
**巡の数が、そのまま「1 人あたり何曲まで」になります。**
同じ人から何曲か取るときは、持っている盤を 1 枚ずつ回ります。

| 場面 | 同じ演者の最多（前 → いま） |
|---|---|
| 全部 85,327 曲 | 2.2 曲 → **1.0 曲** |
| Hardcore 12,537 曲 | 4.4 曲 → **1.0 曲** |
| 演者 30 人 5,263 曲 | 17.8 曲 → **7.0 曲** |
| 演者 5 人 1,667 曲 | 53.4 曲 → **40.0 曲** |

★**狭いところでは下限まで下がっています。** 5 人しかいなければ 200 曲は必ず
重なります。**避けられないときは避けません** ―― シャッフルと同じ考え方です。

★**0.34.0 から、くじはシャッフルと別ものです。**
シャッフルは「回数が少ないほど重い」＝忘れている曲を掘る道具ですが、
一本を組むのは逆で、**その演者の核となる曲**が要ります。目的が違うので分けました。
候補を引くときは**再生回数を見ません。**

★1 組から 2〜4 曲渡すので、**どれを出すかは AI が選びます**（上の 0.34.0 を参照）。

★組んだあと、**何組が重なったかを画面に出します。** 黙って済ませると
「まだ直っていない」ようにしか見えないので。

### 組んだ一本

```
🤖 AI が名前を付けた一本（30曲）  ← 再生リストとして残ります
   ↓
▶ を押すと頭から流れます。1 曲ごとに、なぜその位置に置いたかが出ます
```

```
アーティスト — アルバム　🤖 なぜその位置に置いたか
```

★**再生リストとして残ります。** 気に入ったら取っておけますし、手で並べ替えも、
削除も、m3u への書き出しもできます。

★**組んだ一本は、並んだ順に流します。** シャッフルは自動で切れます。

---
### 🌐 響きタブ

辿ると、3 カラムに「🌐 響き」タブが増えます。

| 列 | 中身 |
|---|---|
| 辿った言葉 | どの言葉から辿って見つかったか |
| 演者 | 手元にあった演者 |
| アルバム | その中のアルバム |

いつもの 3 カラムと同じで、**Ctrl / Shift で複数選べます**。打ち込んで探せます。

★0.39.1 まで、**このタブを開いているだけで**辿って見つかった曲だけに絞られて
いました。それは `sel` に出ないので「✕ 絞りを外す」で外せず、**名前どおりに
働いていませんでした。** 0.40.0 からは、列で選んだときだけ絞ります ―― ほかの
列とまったく同じです。

辿った言葉は、あとから**名前を変えたり、消したり**できます。
この欄は、**「🌐 響き」タブを開いているときだけ**出ます
（ほかのタブを見ているときは、押す用がないので）。

```
🌐 40 組・1,285 曲   [▶ 辿った言葉 4 本] [▶ 交差 17] [× 全部外す]
```

★**手を出す欄は、列で選んだ言葉のぶんだけ**出ます。

```
辿った言葉の列で helmet(03) を選ぶと ―― この 1 行だけ

  helmet(03) 35 [✎] [▶ 確かめる候補 14] [🔀 一本を組む] [×]
```

何も選んでいなければ、欄そのものが出ません。20 本 辿っても 1 行のままです。
全部並べて見比べたいときは「▶ 辿った言葉 4 本」を開きます（高さは 4 行ぶんまで）。

★ここに「一本を作る」ボタンは置いていません。**押すものが並んでいると、
押してしまうからです**（実地で、同じものを何個も作ってしまったと言われました）。
一本を作るのは、上の「一本を組む」だけです。

★0.15.1 まで、**ボタンは消したのに「辿る」の中に同じことが残っていました。**
辿ると一本まで勝手にできていました。いまは辿るだけです。

### 確かめる候補（手元で見つからなかった名前）

```
🌐 3 組・120 曲   [辿った言葉（21）] [✎] [×]   [▶ 確かめる候補 12]   [× 全部外す]
```

押すと開きます。**当たり（鳴らせる曲）が主で、こちらは脇**なので、たたんであります。

#### ★当たりと外れで、照合の厳しさを変えています

| | 何に当てるか | なぜ |
|---|---|---|
| **当たり**（曲を鳴らす側） | 演者の完全一致だけ。**厳しく** | 違う曲を鳴らすほうが困るので |
| **確かめる候補**（持っていないと言う側） | 演者・盤名・曲名に部分一致。**緩く** | 持っているのに買いに行かせるのが一番困るので |

同じ判定をそのまま裏返すと、**アルバム名や曲名としてしか持っていないもの、
コンピレーションで持っているものが、全部「持っていない」に化けます。**

実測（86,044 曲。名前の 4 分の 3 は何らかの形で持っている作り）:

```
名前  22 個 … 厳しいと 15 個が「無い」 → 緩くすると 5 個（10 個は持っていた）
名前 150 個 … 厳しいと 110 個が「無い」 → 緩くすると 37 個
```

★**間違えるなら「持っている」側に間違えます。** 短い名前は盤名や曲名に紛れて
「持っている」に倒れますが、それでよいと決めました。
持っていないものを見落とすより、持っているのに買わせるほうが困るからです。

#### ★「買い物リスト」としては出しません

見つからなかった名前には、**3 種類が混ざっていて、見た目では区別がつきません。**

| | |
|---|---|
| ① | 本当に持っていない |
| ② | 持っているが、名前の書かれ方が違う |
| ③ | AI が実在しないものを出した |

だから「確かめる候補」と呼んでいます。**買う・落とす仕掛けは置いていません。**

#### depth で意味が変わります

| 深さ | 意味 |
|---|---|
| 1 | 入口を持っていない（意外な穴） |
| 3 以上 | まだ辿り着いていない場所（探索の先端） |

並びは当たりと同じで**重い順**（深く・新しいほど上）。

★**JSON に足りない欄はありませんでした。** name / depth / description と、
木ごとの keyword / savedAt だけで足ります。Resonance 側の変更は要りません。

---

★消えるのは辿った記録だけです。**音楽ファイルには触りません。**

---
### 決まった形の JSON を読み込む

音蔵は、下の形の JSON を読める作りになっています（辿った木を外から持ち込むため）。
★**入り口は公開版では出していません。** 0.10.0 で中から辿れるようになったので、
いまは要らないためです。形と処理は残してあるので、必要になれば出せます。

```json
{
  "version": 1,
  "exportedAt": "2026-08-29T01:57:44.435Z",
  "entries": [
    {
      "keyword": "辿った言葉",
      "savedAt": "2026-08-29T01:56:16.575Z",
      "nodes": [
        { "name": "アーティスト名", "genre": "music", "description": "なぜ繋がるか", "depth": 3 }
      ]
    }
  ]
}
```

★作者の別のアプリ **Kokoro OS の Resonance** が、この形で書き出します
（Resonance は未公開です。無くても、上の「言葉から辿る」で同じことができます）。

### ★`name` は artist だけに当てます（実測で決めました）

木のノード名を、曲の何と突き合わせるか。実物（木 7 本・147 ノード）と
ライブラリ 86,044 曲で数えて決めました。

| 当てる先 | 当たった music ノード |
|---|---:|
| **artist だけ** | **22 個** |
| artist + album | 22 個（**album を足しても増えない**） |
| 3 つ全部 | 22 個（**title を足しても増えない**） |

`title` を足すと全体では 13 個増えますが、中身は**全部 music 以外の偶然の同名**でした。

```
ある思想の名前   [other] 曲名として 2 曲
ある芸術家の名前 [art]   曲名として 1 曲
ある場所の名前   [place] 曲名として 1 曲
ある映画の題名   [movie] 曲名として 5 曲
ある映画の題名   [movie] 盤として  10 曲
```

曲名は短い普通の言葉が多く、**別のものと偶然一致します**。当たっても意味がありません。
**`genre` が `music` のノードだけ**を拾います。

### 重みのつけ方

**深いほど強く、新しいほど強く**しています。

- 深さ 0〜3 で 1〜4 倍（遠いところの当たりのほうが、発見として面白いので）
- 新しさは 60 日で半分になる緩やかな減り方（急に切ると古い木が死ぬので）
- 同じ名前が 2 本の木にあれば、**重いほうの理由**を採ります

### 速さと置き場所

突き合わせは **86,044 曲を 1 周するだけ**です（実測 25 ミリ秒）。
名前ごとに全曲を探すと 22 倍になるので、曲の側を 1 回なめて引いています。

```
C:\Users\（自分）\AppData\Roaming\Otogura\resonance.json
```

辿った木と、読み込んだ木は**同じところに貯まります**。「× 響きを外す」で消せます。
**消えるのはこの控えだけ**です。

---

## ★このアプリで唯一、外に通信するところ

音蔵はもともと、**手元だけで動きます**。この機能だけが例外です。

| | |
|---|---|
| **🎧 気分から一本を組むとき** | ジャンル名の一覧（件数つき）／手に入れた年の一覧／打ち込んだ気分の文／**候補 200 曲の曲名・アーティスト名・アルバム名** |
| 　★このとき**演者の一覧は送りません** | AI が自分の知識で挙げた名前を、手元で突き合わせるだけです（0.35.0） |
| **🌱 言葉から辿るとき** | 打ち込んだ言葉／★**この人の演者名を最大 800 組**（曲数つき）／蔵書の大きさ（組数・曲数） |
| 　なぜ演者名を送るか | 「この人の蔵書に合わせて辿る」ためです。送らないと、手元と無関係な名前ばかり返ります |
| **どちらでも送らないもの** | **ファイルのパス・音そのもの** |
| **いつ** | 「選んでもらう」を押したときだけ。裏では通信しません |
| **既定** | **切**。キーを入れるまで、欄そのものが出ません |
| **使う型** | `claude-opus-5` |

★**曲を全部渡すことはできません。** 作者のライブラリで数えると、
**86,044 曲 = 約 210 万トークン**。文脈の上限（100 万）に入りません。
渡しているのは**ジャンル名の一覧だけで、約 725 トークン**です。

### キーの置き場所

**画面側（renderer）はキーに触れません。** 本体（main）だけが持ちます。

保存先は次の 1 ファイルで、**Windows の暗号化（DPAPI）をかけて書きます**。

```
C:\Users\（自分）\AppData\Roaming\Otogura\ai-key.bin
```

★**平文では保存しません。** 暗号化できない環境では、保存そのものを断ります。
★キーを読み返す窓口はアプリ内に**作っていません**（画面から読めると、盗める形になりうるので）。
★消すときは「🔑 APIキーを消す」（同じボタンです）を押してください。消えるのはキーのファイルだけです。

キーは [console.anthropic.com](https://console.anthropic.com) で作れます。
費用はジャンル一覧だけなら **1 回あたり 1 円未満**です。

---

### 3 カラムのタブ

3 カラムの上のタブで、何で絞るかを切り替えられます。

| タブ | 3 カラムの中身 | いつ出るか |
|---|---|---|
| ジャンル / アーティスト / アルバム | これまでどおり | いつも |
| 日付（年 / 月 / 日） | 手に入れた時期で絞る | いつも |
| 🏷 ジャンル（まとめ） | AI がまとめた組で絞る | まとめたあと |
| 🌐 響き | 辿った言葉 / 演者 / 盤で絞る | 辿ったあと |

**★効くのは、開いているタブの選択だけです。**
日付タブに切り替えれば、ジャンルの選択は効きません（消えてはいません）。
どのタブも Ctrl / Shift で複数選べます。

隠れているタブに残っている数は `● 3` のように出しますが、**それは効いていません。**
押して戻れば、また効きます。

★以前は掛け合わせになっていました（0.5.0〜0.39.1）。タブが 2 つのころは
それで良かったのですが、4 つになってからは「なぜこれしか出ないのか分からない」
の正体になっていたので、0.40.0 でやめました。

★まとめてあれば、開いたときは 🏷 のタブから始まります。

★日付の列は**新しい順**に並びます。粒度は作者の手元でこうなりました。

| | 塊の数 | いちばん大きい塊 | 中央 |
|---|---:|---:|---:|
| 年 | 24 個 | 15,397 曲 | 1,079 曲 |
| 月 | 222 個 | 11,559 曲 | 113 曲 |
| 日 | 1,691 個 | 6,758 曲 | 12 曲 |

いちばん大きい「日」の塊（2014-09-12 の 6,758 曲）は、**まとめて取り込んだ日**です。
「あの時期に入れたやつ」で辿れます。

★日付の分からない曲は、日付で絞ると出ません。日付タブを「すべて」にすれば戻ります。

---

### 絞り込みは複数選べます

3 カラムは **Ctrl / Shift を押しながらクリック**で複数選べます。

| 押し方 | どうなるか |
|---|---|
| ふつうのクリック | その 1 つだけにする |
| **Ctrl+クリック** | 1 つずつ足す。**もう一度押すと外れます** |
| **Shift+クリック** | 直前に押したところからの範囲 |
| 「すべて」 | その列の選択を外す（絞らない） |

選ぶと「すべて（N）」の右に**いくつ選んでいるか**が出ます。

★上の列を選び直すと、下の列の選択は外れます。
「ジャンルを 3 つ ＋ そこに無いアーティスト」のような**実体の無い組み合わせ**が残ると、
なぜ 0 件なのか分からなくなるからです。

### シャッフルに入れたくない曲

一覧の行の「**シャッフルに入れない**」を押すと、その曲は**くじで選ばれなくなります**。

**「一覧から外す」とは別物です。**

| | 一覧に出る | 押せば鳴る | シャッフルで選ばれる |
|---|---|---|---|
| ふつうの曲 | ○ | ○ | ○ |
| **シャッフルに入れない** | ○（薄く 🚫 が付きます） | ○ | **×** |
| 一覧から外す | × | × | × |

**並び順で送るとき（⏭ や自動送り）は流します。** 外れるのはくじだけです。
「聴きたくない曲」ではなく「**不意に流れてほしくない曲**」のための仕組みなので。

まとめて選んでから、下の道具の欄でまとめて外す／戻すこともできます。
指定は覚えておくので、アプリを閉じても残ります。

---

### 「日付」は何の日付か

一覧の「日付」は、**ファイルの更新日時**です。押すと**新しい順**から並びます。

★**作成日時は使っていません。** 実測で使いものにならなかったからです（2026-08-29、標本 400 件 × 2 か所）。

| | ばらけ方 | 使えるか |
|---|---|---|
| 作成日時 | `E:\iTunes Music` の 400 件が**全部 2026-08** | ✗ ディスクをコピーした日に上書きされている |
| **更新日時** | **112 種類の月**（2014-09 / 2015-07 / 2017-04 …） | ○ コピーしても保たれ、手に入れた時期に沿う |
| タグの年 | 400 件中 106 件が**そもそも空** | ✗ これは発売年で、手に入れた日ではない |

作者の手元（`Documents\MP3` の 1,630 曲）だと、こう散らばります。

```
2008 ██ 39      2016 ██ 33      2022 █ 18
2009 ███ 54     2017 ██ 24      2025 █████████████ 245
2010 █ 7        2018 ██ 26      2026 █████████████████████ 1058
2013 ██ 29      2019 ████ 67
```

★**タグを直しても日付は動きません。** 書く前に控えて、書いたあと戻しています。
そうしないと、ジャンルを直した曲だけが「いちばん新しい曲」として先頭に出てきます。

★日付の分からない曲は「—」と出て、新しい順のときは**いちばん下**に回ります。

★読み込みは増えません。覚え書きの照合ですでにファイルの情報を見ているので、
**そこから取っているだけ**です。

---

### シャッフルの対象は、いま絞り込んでいる範囲

3 カラムでアーティストやアルバムを選べば、**その中でシャッフル**します。
選び直せば、**止めずにそのまま**対象が変わります。何も選んでいなければ全曲が対象です。

★以前は「押した時点の一覧」を覚える作りでした。
再生中に絞り込みを変えても次の曲が変わらないように、という意図でしたが、
**全体シャッフルに戻すのに一度止める必要があり**、本人から作り直しの希望が出ました（2026-08-29）。

★一覧は 2,000 行までしか描きませんが、**対象はそこで切りません**。
「見えている 2,000 曲」ではなく「いま絞り込んでいる範囲」が対象です。

---

### ★ファイルは消しません

一覧の「削除」は**一覧から外すだけ**で、ファイルそのものは残ります。
このアプリのコードには、ファイルを消す処理を**一行も書いていません**。

唯一の例外はタグの編集で、これだけはファイルを書き換えます。
そのときも、**渡した欄以外のタグは触りません**（`node-id3` の `update` を使い、
`write` は使わない）。音のデータが変わっていないことは `check-tags.js` で確かめています。

★**タグを書き換えられるのは MP3 だけです。** `node-id3` が ID3 タグ専用のためです。
`.m4a` を選んだときは、書く前に断って飛ばします（**再生と読み取りはできます**）。

---

## 大きなライブラリについて

作者のライブラリ（**171,085 曲 / 387 GB**）で、実際に測って直しました。

| | 直す前 | 直した後 |
|---|---|---|
| アートワーク | 全曲ぶんを保持（**17.8 GB**。これで落ちていた） | 再生する 1 曲だけ |
| 2 回目以降 | 毎回すべて読み直し | **変わったファイルだけ** |
| 途中で閉じたら | その回の読み込みを**まるごと破棄** | **30 秒ごとに保存。続きから** |

### タグ読みの速さ（★数字の読み方に注意）

| | 1 曲あたり | 171,085 曲だと |
|---|---|---|
| 初めて触るファイル | **約 36 ms** | **約 100 分** |
| 一度読んだファイル | 約 1 ms | 約 3 分 |

★**この 2 つを混ぜないでください。** 作者は一度混同しました。
同じ標本で 2 回続けて測り、「設定を変えたら 24 倍速くなった」と読み違えたのです。
実際は **OS がファイルを覚えていただけ**で、設定は関係ありませんでした。
速さを比べるときは、**どちらも初めて触るファイル**で測ってください。

なお、同時に読む本数（8 / 16 / 32 / 64 / 128）も測りましたが、**差はありませんでした**。
待っているのはディスクそのものなので、増やしても速くなりません。

### ★半分は曲ではなかった

作者のライブラリを数えたら、**171,092 件のうち 85,039 件が曲ではありません**でした。

macOS から Windows 系のディスクへコピーすると、本体と対になる小さな付随ファイル
（`._` で始まるもの）ができます。音は入っていません。

- 読み込みに**倍**の時間がかかっていた（103 分 → **52 分**）
- 一覧が埋まって使いものにならなかった

**数えるまで、ただ「曲がたくさんある」ようにしか見えませんでした。**
いまは `._` で始まるものを飛ばします。実際の曲数は **86,057** でした。

### 待たせない工夫

はじめの登録には時間がかかります。ただし:

- **前回の一覧はすぐ出ます**（覚え書きから。待ち時間ゼロ）
- 読み直しは**裏で進みます**。見つかったそばから一覧に出ます
- 進み具合と**残り時間**を下端に出します
- **途中で閉じても、次に開いたとき続きから**進みます
- **途中で止められます**（下を参照）

### ★重くなったら、止められます

大きなライブラリを読み込むと、**ディスクを長く占め続けます**
（作者の 86,000 曲で約 50 分）。CPU ではなくディスクが詰まるので、
ほかのアプリの読み書きまで遅くなります。

走査中は「再スキャン」が「**■ 止める**」に変わります。押せばそこで切り上げます。

★**止めても損はしません。** そこまで読んだぶんは残り、まだ見ていないぶんは
前の記録から補うので、**一覧は減りません**。覚え書きも減りません。
**次に開いたときは続きから**進みます。

★**数えている最中でも止まります。** 大きなライブラリでは数えるだけで
297 秒かかるので（実測）、読み始めてからしか止まらないと、そのあいだ止められません。

★窓を閉じても止まります。走査は本体の中で走っているだけで、
**別のプロセスも常駐する仕掛けもありません**。閉じればプロセスごと終わります。

一覧は一度に 2,000 行までしか描きません（それ以上は上の 3 カラムで絞ってください）。
省いた件数は画面に出ます。

★この 2,000 という数字も**測って決めました**（500 行 = 39 ms / 2,000 行 = 146 ms /
85,000 行 = 5,915 ms）。最初は測らずに 500 と決めていて、絞り込んでも足りませんでした。

---

## 動作を確かめる

画面を開かずに、計算部分だけを検査できます。

```
npm run test-music     # 検査用の MP3（無音）を作る
npm run check          # 下の 18 本をまとめて走らせる
```

| 検査 | 見ているもの |
|---|---|
| `check-shuffle.js` | シャッフルが、忘れている曲を選ぶか |
| `check-loudness.js` | 曲ごとの音量差をならす計算 |
| `check-playlist.js` | 再生リストの保存・掃除・m3u |
| `check-url.js` | 変な文字を含むパスでも再生できるか |
| `check-tags.js` | タグを書いても**音のデータが変わらない**こと |
| `check-cache.js` | **取り込み忘れ・黙って捨てる catch・画面と本体のつなぎ・名前を変えたときの引き継ぎ** |
| `check-sort.js` | 並び順（作った台 ＋ **手元のライブラリ全曲**） |
| `check-stop.js` | 走査を止めても、**フォルダが無くても**、一覧と覚え書きが減らないこと |
| `check-ai.js` | AI に渡す頼み文と、**返ってきたものの照合** |
| `check-resonance.js` | 響きの木の読み込みと、曲との突き合わせ |
| `check-knob.js` | **つまみが 2 つの道すべてに効くか。候補より多い曲数を頼まないか** |
| `check-genre.js` | 散らかったジャンル名をまとめる |
| `check-own.js` | 自分の音源を、シャッフルからも AI からも外せているか |
| `check-naoshi.js` | 手直しの層（`手直し.json`）。**手で直したほうが必ず勝つこと** |
| `check-hist.js` | 打った文の履歴と、その消し方 |
| `check-lang.js` | **画面に出る文すべてに英語の訳があるか。頼み文が本当に英語になるか。並べて読み込む台本が同じ名前を作っていないか** |
| `check-en.js` | **英語にして、本物の画面を開いて読む**（音蔵を開いたままでも走ります） |
| `check-ui.js` | 画面を実際に開いて読む（**音蔵を閉じてから走らせてください**） |

★`check-cache.js` は、実際に起きた不具合から生まれました。どれも**動かすまで気づけない**種類です。

| 起きたこと | 見えた症状 |
|---|---|
| 取り込み（import）の 1 行を書き忘れ、`catch` が握りつぶしていた | 直した直後は正しいのに、**開き直すと元に戻る** |
| 画面が「戻せます」と案内する操作が、**どこにも無かった** | 一度外した曲が二度と戻らず、**足し直しても出てこない** |

2 つめは、`preload` に窓口があり、本体に処理もあり、**画面からだけ呼ばれていない**状態でした。
だから検査は**両方向**を見ます ―― 画面が呼ぶものが向こうにあるか、
そして**向こうが出しているものが、すべて呼ばれているか**。

自分のライブラリで走査の速さを測るときはこちら。

```
node check-scan.js "D:/Music"
```

**リポジトリに音楽ファイルは入っていません。**
`npm run test-music` が、その場で無音の MP3 を作ります。

---

## どう作ったか

このアプリは、**指示を書いた人が一行もコードを書いていません。**

1. **[Kokoro OS](https://kokoro-os-v2.vercel.app) の Baton** で、作りたいものを書き、
   「まだ決まっていないこと」に答えて**指示書**を作った
2. その指示書を **Claude Code** に渡して実装させた
3. 実際に使って出てきた不具合を、そのつど直した

★**指示書が聞き漏らしていたこと**も分かりました。

| 漏れ | 何が起きたか |
|---|---|
| 再生の始め方 | 押す場所が分からず、しばらく再生できなかった |
| 曲が終わったらどうなるか | 次へ・繰り返し・シャッフルが丸ごと抜けていた |
| **扱う曲数** | 4 曲で試して満足し、1,000 曲で画面が崩れ、170,000 曲で落ちた |
| **隠したものの戻し方** | 「隠す」だけ作られ、**戻す操作が無かった**（案内文だけあった） |

この経験をもとに、Baton は「**操作**（どう始めて、どう終わるか）」と
「**量**（何件扱うか）」を聞くようになりました。

コードの中には、**なぜそう書いたか**と**何を間違えたか**が日本語で書いてあります。
`★` の付いた注記が、実際に踏んだ失敗の記録です。

---

## ライセンス

MIT License（[LICENSE](LICENSE)）。

依存しているものも、すべて MIT / ISC / BSD / Apache-2.0 です
（`electron` / `music-metadata` / `node-id3` ほか 89 個）。

---

## 版ごとの記録

ここから下は、版ごとに**何を直したか・何を測ったか・何を間違えたか**の記録です。
新しいものが上にあります。

★`★` の付いた行が、実際に踏んだ失敗です。
直した話より、**なぜそう直したか**のほうを厚く書いてあります。

### 0.43.1 で直したこと

**走査中にやった設定変更が、走査の終わりに巻き戻っていました。**

> プレイリストをいくつか消す → 新たに AI DJ にプレイリストを作らせる →
> 新しいプレイリストが生成されたけど表示されない → よく見ると消したはずの
> プレイリストがある → そのプレイリストを消すと、先程消したプレイリストが
> 全て消えて新しく生成したプレイリストが出てくる

★原因はこれでした。

```js
const s = await 設定を読む();          // ← 走査を始める前に読む

… 93,856 曲の走査。数分かかる …

if (掃除.落とした) { s.lists = 掃除.lists; await 設定を書く(s); }   // ★古い s を丸ごと書き戻す
```

```
① 走査 開始（このときの設定を s に持つ）
② 再生リストを削除          → ディスクは正しく更新
③ AI が一本を作る            → ディスクは正しく更新
④ 走査 終了 → 古い s を書き戻す ← ここで ②③ が消える
⑤ 何か 1 つ削除する          → 初めてディスクを読み直すので、正しい状態が出る
```

**「そのあと 1 つ消したら直った」のは、そこで読み直したからです。**
壊れてはいませんでした ―― 一度 巻き戻って、読み直しで揃っただけです。

★**巻き戻っていたのは再生リストだけではありません。**書き戻していたのは設定
まるごとなので、走査中にやったことは何であれ巻き戻り得ました。

| 巻き戻り得たもの |
|---|
| 再生リストの作成・削除・並べ替え |
| フォルダの追加・削除 |
| ジャンルのまとめ |
| つまみ（幅・量・強度・拡大解釈） |
| 一覧から外した曲 ／ 自分の音源の指定 |

蔵が大きいほど走査が長く、**この窓は起動のたびに数分ひらいていました。**

★直し方は 1 つです。**書く直前に読み直して、触る欄だけ差し替える。**
長く持った設定を、丸ごと書き戻さない。

★見張りを 2 つ足しました。新しい形になっていることと、**古い形に戻っていないこと**の両方を見ます。

### 0.43.0 で変わったこと（0.43.1 にも入っています）

**いま鳴っている曲から、押すだけで辿れるようになりました。**

> 友人で音楽を探すのが苦手な人がいていつも僕に聞いてくるんですが、
> この前 AI に相談してオススメを聞いたらみんなハズレで困ったと聞いて、
> 何をオススメされたか聞いたら全部的外れだったんです。
> だから、プレイヤーを通じて「今聴いてるので他にオススメある？」って
> 聞けると、その友人も困らないのかな？って思いました。

再生バーに、演者名の入った札が 1 つ増えます。

```
🌱 Helmet から辿る    ⏮  ▶  ⏭  🔁 しない  🔀 オフ …
```

★足したのは**入口だけ**です。押すと、欄に演者名を入れて「辿る」を押したのと
**まったく同じこと**が起きます。辿る道は 1 本のままにしてあります。

| | |
|---|---|
| 手元にあった演者 | 🌐 響きタブに集まります → そのまま聴けます |
| **手元に無かった名前** | **「確かめる候補」に出ます → 探しに行く名前** |

★**「何と打てばいいか分からない」を消すのが要点**です。辿るには演者名を 1 つ
入れる必要がありましたが、鳴っている曲があるなら、それはもう決まっています。

★札に演者名を出します。押すものが増えるので、**押し間違えても何が起きるか
読める**ようにしました。AI が使えないとき・演者名が無いときは出しません。

---

★なぜ「起点に演者名を置く」のが効くのか、実測があります（2026-09-04）。

```
外れやすい： 「ロックでおすすめある？」    ← ジャンルは何も決めない
当たる：     「Helmet みたいなのある？」   ← 1 組の名前が全部を決める
```

「Helmet みたいなバキバキのオルタナロック」で 3 回 試したところ、挙がった名前は
**3 回とも実在**し、手元に無かったものは Barkmarket / Season to Risk / Shiner /
Rapeman / Cop Shoot Cop ―― どれも的確でした。**「持っていない」が、そのまま
「探しに行く名前」になります。**

このジャンル名が当てにならないことも測ってあります。この蔵では `Rock` の札が
付いているのは 4% で、中身はばらばらでした。**「ロックが好き」では何も決まりません。**

### 0.42.0 で変わったこと（0.43.1 にも入っています）

**組む範囲が、画面を見なくなりました。**

> やっぱりAI DJや響きで生成を誤操作するので
> AI DJと響きは画面に表示されてるリストを参照しない。内部でリストを参照する

★0.40.0 で「隠れた絞り込み」は潰しましたが、**タブを開いているという状態そのものが
範囲を決める**構造は残っていました。響きタブを見ているだけで、組む先が変わる。
それが生成の誤操作の、最後の源でした。

**決まりが 2 つに分かれます。**

```
見えているもの  ＝  鳴るもの        （再生・シャッフル。今までどおり）
ジャンルの選択  ＝  組まれるもの     （AI DJ・響き。画面は見ない）
```

| 何で絞ったか | 再生・シャッフル | AI DJ・響き |
|---|---|---|
| ジャンル（まとめ／元の名前） | 効く | **効く** |
| アーティスト | 効く | **効かない** |
| アルバム | 効く | **効かない** |
| 日付（年 / 月 / 日） | 効く | **効かない** |
| 響きの列 | 効く | **効かない** |
| どのタブを開いているか | 一覧が変わる | **関係ない** |

★ジャンルは**まとめ優先**です。まとめてあればその組、無ければ元の名前を見ます。
実測で、元の名前は 98 個に散らばっていて `Rock` が受け皿になっており、
AI の検索から漏れます。まとめれば 18〜22 組です。

★ジャンルを選んでいなければ、**ライブラリ全部**から組みます。

**押したボタンが、範囲を決めます。**

| 押したもの | 組む範囲 |
|---|---|
| 気分から「一本を組む」 | ジャンルの選択（無ければ全部） |
| AI が演者を挙げたあと | **その演者たち**（内部で持ちます） |
| 響きの「🔀 一本を組む」 | **その言葉で辿ったもの**（タブは関係なし） |

★AI が挙げた演者から組む、という要の動きは残しています。画面に頼らず、
内部で持つようにしました。実測で、演者で絞れたときの当たりは桁違いに良いためです。

★**見えなくしたわけではありません。**「組む範囲」の欄は、
実際に効いている絞り込みをそのまま出します。

```
組む範囲: 20,418 曲（ライブラリ全部）
組む範囲:  3,120 曲（ジャンル（まとめ）: Alternative・Crust Hardcore ほか2）
```

「見える」ことと「操作で変わる」ことを、切り離しました。

★あわせて「自分で絞っているか」の判定も、ジャンルだけを見るようにしました。
前は演者や日付で絞っただけでも AI にジャンルを選ばせなくなっていて、
これも誤操作の元でした。

### 0.41.0 で変わったこと（0.43.1 にも入っています）

**消した響きが、戻せるようになりました。**

実地で、辿った木 4 本が消えていました。押したかどうか本人にも記憶が無く、
**記録が何も残らないので、後から判別できませんでした。**

★このアプリで最初に決めたことが、ここにだけ効いていませんでした。

> 音楽ファイルを消さないでください。一覧から外すだけにしてください

曲と違って、辿った木は**元から作り直せません**（AI に何度も問い合わせて作るため）。
それが押し間違い 1 回で永久に失われるのは、いちばん割に合いません。

```
前）× 全部外す  →  resonance.json を消す。記録は何も残らない
後）× 全部外す  →  resonance-捨てた.json へ移す。消さない
                   下に「↩ 消した響き 4 本を戻す」が出る
```

| | |
|---|---|
| 「× 全部外す」 | 消さずに退避します |
| 言葉ごとの「×」で**最後の 1 本**を消したとき | 同じく退避します |
| 戻すボタン | 「↩ 外した曲を戻す」の隣。**同じ場所・同じ形**にしました |
| 退避したものが無いとき | ボタンごと出しません |

★戻すときは、そのとき辿っているものを**入れ替えで**退避します。
どちらか一方が必ず残るので、行き来できます。

★持つのは **1 つ前だけ**です。世代を増やすと「どれが要るのか」が分からなくなります。

★すでに失われたものは戻りません。この版から先が守られます。

### 0.40.1 で直したこと（0.43.1 にも入っています）

**響きの編集欄が、辿った言葉の数だけ伸びていました。**

> 別ウインドウが目的ではなく、響きをたくさんやると編集欄がたくさん並んで
> 下の画面を圧迫するのでそれを回避したいという意味で、案として提案しました。
> 目的は画面の圧迫の回避と並びの美しさでした。

★**並べていた一覧は、すぐ下の「辿った言葉」の列とまったく同じもの**でした。
同じ一覧が画面に 2 つあり、片方だけが言葉の数だけ伸びていました。

```
前）4 本 → 4 行。20 本 辿れば 20 行

  🌐 40 組・1,285 曲  [▼ 辿った言葉 4 本] [▶ 交差 17] [× 全部外す]
  helmet(03) 35 [✎] [▶ 確かめる候補 14] [🔀 一本を組む] [×]
  helmet(02) 38 [✎] [▶ 確かめる候補  7] [🔀 一本を組む] [×]
  helmet(01) 35 [✎] [▶ 確かめる候補  3] [🔀 一本を組む] [×]
  helmet     29 [✎] [▶ 確かめる候補  1] [🔀 一本を組む] [×]

後）何本 辿っても 1 行

  🌐 40 組・1,285 曲  [▶ 辿った言葉 4 本] [▶ 交差 17] [× 全部外す]
  helmet(03) 35 [✎] [▶ 確かめる候補 14] [🔀 一本を組む] [×]
       ↑ 列で helmet(03) を選んでいるので、この 1 行だけ
```

**列で選ぶ。欄で押す。**に分けました。

| 状態 | 欄の高さ |
|---|---|
| 何も選んでいない | 出ません |
| 列で 1 つ選んだ | 1 行 |
| 列で 3 つ選んだ | 3 行 |
| 「辿った言葉 N 本」を開いた | 全部（**4 行ぶんで打ち止め**、あとは中で送る） |

★`🔀 一本を組む` は、元から「その言葉を列で選んでから組む」動きでした。
**欄がそれを二重に持っていただけ**です。押したときの結果は変わりません。

★開いたときの高さの上限も、画面の 4 割（40vh）から 4 行ぶんに縮めました。
あれが下の一覧を半分近く押し下げていました。

### 0.40.0 で変わったこと（0.43.1 にも入っています）

**絞り込みを、見えているものだけにしました。**

> 操作が煩雑で自分で色々試して仕様を調べないと正しく動かすことができない
> 何かの仕様を捨てないとシンプルで直感的な操作ができない気がしています
> まず、検索するタブを一つにした方がいいですよね？

★範囲が **5 か所**から集まっていて、うち **2 つは画面に出ていません**でした。

```
① 見えているタブの選択          … 見える
② 隠れているタブの選択          … 見えない  ← 捨てた
③ 響きタブを開いていること自体  … 見えない  ← 捨てた
④ タグ無しを隠す設定            … 見える
⑤ 再生できない曲を除く          … 見える
```

**② 隠れているタブも効く**のは 0.5.0 で入れたものです。タブが 2 つ（ジャンル・
日付）のころは掛け合わせに意味がありましたが、4 つになり、③ が加わってからは
「なぜこれしか出ないのか分からない」の正体になっていました。
★掛け合わせは使っていないと確かめたうえで捨てています。

**③ 響きタブにいるだけで絞られる**のは `sel` に出ないので、
「✕ 絞りを外す」で外せませんでした。**名前どおりに働いていませんでした。**
いまは「辿った言葉」の列で選んだときだけ絞ります ―― ほかの列とまったく同じです。

**いまの決まりは 1 行です。開いているタブの選択だけが効く。**

---

**開いたとき、最初に出るタブを「ジャンル（まとめ）」にしました。**

> 僕が体感的に使ってほしいのはAIにお願いしてジャンルをまとめたタブです。
> デフォルトはジャンルが散らばりすぎてAIの検索から漏れるものが多いからです。

```
そのままのジャンル   98 個   "Rock" が受け皿になり、中身がばらばら
                            Grindcore / GrindCore のような揺れもある
まとめたジャンル     18 組   まとめてあれば、こちらから始まります
```

★まとめていなければ、今までどおりジャンルのタブから始まります。
★自分でタブを押したあとは、勝手に切り替えません。

---

**AI にも、まとめた名前を渡すようにしました。**

せっかくまとめても、AI には散らかったままの 98 個を見せていました。
ここが、選んだジャンルの外の曲が混ざる原因のひとつでした。
AI が答えたジャンルも、まとめの列に当てはめます（前はジャンルの列に入れていて、
まとめのタブを見ていると絞りが効きませんでした）。

---

**画面の文が古い決まりを喋っていたのも直しました。**

```
前  「このタブで 2 個選んだままです（絞り込みは効いています）」
後  「このタブに 2 個残っています（いまは効きません。押すと効きます）」
```

「組む範囲」の欄も、効いている選択だけを並べます。
前は隠れたタブの選択まで書いていたので、**曲数と食い違って**いました。

### 0.39.1 で直したこと（0.43.1 にも入っています）

**つまみを動かすだけで、演者の数が変わっていました。**

> スライダーを動かすだけでバンドの数が変わったのですが、どうしてですか？
> 生成しなくてもスライダーを動かすと内容が変わるのですか？

★**動いていたのは「数」ではなく「持ち主」**でした。全体は変わっていません。

```
つまみ左   75 組・3,540 曲   helmet(05) を選ぶと 演者 11
つまみ右   75 組・3,540 曲   helmet(05) を選ぶと 演者  6
```

同じ名前が何本かの木に出たとき、**持ち主を「重いほう」に決めていました。**
その重みが 0.36.0 でつまみに繋がったので、**動かすたびに持ち主が入れ替わって**
いました。確かめる候補の数が行ごとに 増え減りしていたのも、同じ理由です。

★つまみは**並び順**のためのものです。持ち主は目盛 3（平ら）で決めるようにしました。
つまみを動かしても、どの言葉が何を見つけたかは変わりません。

### 0.39.0 で変わったこと（0.43.1 にも入っています）

**2 つのつまみを、1 つずつの役目に分けました。**

本人の整理（2026-08-31）:

> 拡大解釈オフ（AIは頑張らない）
> ・王道（短い枝）↔外す（長い枝）
> 拡大解釈オン（AIは頑張る）
> ・王道（短い枝）↔外す（長い枝）

★0.38.0 では、**王道が「短い枝」と「頑張らない」の両方**を担っていました。
混ざっていると「短くしたいが数はほしい」が言えません。

```
王道 ↔ 外す   枝の長さ（1 段 ↔ 3 段）だけ
拡大解釈      数を埋めるかどうか（頑張らない ↔ 頑張る）だけ
```

#### 4 通りになります

| | 拡大解釈 切（頑張らない） | 拡大解釈 入（頑張る） |
|---|---|---|
| **王道**（1 段） | 近いものだけ、少しだけ | **近いものを、数そろえて** |
| **外す**（3 段） | 遠くまで、少しだけ | 遠くまで、数そろえて |

★左上が「軸のそばを、無理せず」。**右上が新しくできた形**です ―― 
枝は短いまま、数はそろえる。車線が厚い言葉では、ここがいちばん濃くなります。

★右下がこれまでの既定（3 段・数を埋める）。左下は「遠くの発見を、少しだけ」。

#### 頑張らせないとは

```
切  ・数は多くても N 個。★足りなければ少なくて構いません
　　・★数を埋めるために遠いものを足さないでください
　　・★近さだけで選んでください。一覧に寄せると、そこから外れます

入  ・できるだけ多くの、別々のバンドを。N 個ほしいところです
　　・上の一覧にある名前を選んで構いません。むしろ歓迎します
```

★「一覧」はこの人の蔵書です。**寄せると蔵書の色が返ってきます。**
頑張らせないときは、そこから離します。

### 0.38.0 で変わったこと（0.43.1 にも入っています）

**王道のときは、AI に頑張らせないようにしました。**

> やっぱり拡大解釈が多く含まれている印象です。
> AIが頑張った結果なのかな？頑張らないようにしたいです。

★そのとおりでした。王道でも、頼み文はこう言っていました。

```
・起点の言葉から、**3 段の木**を作ってください
　全部で 35 個ほどになります
```

**深さ 3 は、定義上いちばん遠いところです。** そして数を指定すれば、
足りないぶんは**遠くから埋めるしかありません。**
実際、王道で 4 回辿って毎回 35〜39 個返ってきていました。

いまは、つまみで木の形そのものが変わります。

```
王道      1 段で構わない。多くても N 個。★足りなければ少なくてよい
　　　　  ★数を埋めるために遠いものを足さない
やや王道  2 段まで。多くても N 個
ふつう〜  3 段の木。全部で N 個ほど（これまでどおり）
```

#### 一覧を、桁のそろった板にしました

> 確かめる候補や一本に組むのリスト化ですが、別ウインドウで綺麗に並ぶ感じが良いです

★**別の窓にはしませんでした。** 窓を増やすと、閉じ忘れ・行き違い・
どちらが本物か、という面倒が増えます（この作りは窓 1 枚で通してきました）。

欲しいのは「綺麗に並ぶ」ほうなので、**下に広がる板**にして桁をそろえました。
確かめる候補が 0 本の言葉でも桁は空けてあります ―― 行ごとにボタンの位置が
ずれると読みにくいので。

### 0.37.1 で直したこと（0.43.1 にも入っています）

**辿ったのに結果が出ず、立ち上げ直すと出てくる、を直しました。**

> 響きで生成したのに生成結果が出なかったので立ち上げ直したら反映されました。
> 2 回確かめてあります。

★原因は 2 つ。どちらも 0.37.0 で「混ぜるのをやめた」ときの詰め残しです。

**① 混ぜるのをやめたのに、返り値がまだ混ぜた結果を見ていました。**
ReferenceError で窓口が落ちます。**保存はその前に済んでいる**ので、
立ち上げ直すと出てきます ―― 報告のとおりの症状です。

★構文としては正しいので `node --check` は通ります。**走らせないと分からない類**でした。

**② 保存した言葉は `helmet(01)` になりうるのに、画面は打った言葉で絞っていました。**
0 件になって、辿ったのに何も出ません。保存された名前を返して、それで絞るようにしました。

見張りを 2 本足しました ―― 混ぜるのをやめた残りが無いこと、保存した名前を返していること。

### 0.37.0 で変わったこと（0.43.1 にも入っています）

**響きを、辿った言葉ごとの一覧にしました。**

本人の方針（2026-08-31）:

> 確かめる候補が一つにまとまっているので、生成ごとに確かめるの候補を分ける。
> 同じ単語で響きを生成した場合、別のものとする「helmet」「helmet(1)」みたいな感じで。
> そうすると今の響き編集項目のバーの部分が煩雑になるので、リスト化してボタンか何かで隠す

#### 同じ言葉でも、生成ごとに別のものとして残します

```
helmet       39   ✎  ▶ 確かめる候補 13   🔀 一本を組む   ×
helmet(01)   17   ✎  ▶ 確かめる候補  5   🔀 一本を組む   ×
fugazi       24   ✎  ▶ 確かめる候補  9   🔀 一本を組む   ×
```

★前は**混ぜていました。** つまみが無かったころは、それが正しかったのです
（「同じ言葉で押しても何も変わらない」という不満から入れた手当てでした）。

いまは**つまみで結果が変わります。** 混ぜると「王道の helmet」と
「外すの helmet」がひとつに潰れて、比べられません。だから別々に残します。

★あわせて「すでに挙げた名前」を渡すのもやめました。
渡すと 2 本目が 1 本目の**補集合**になり、王道で辿り直したのに
1 本目に出た Helmet 自身が外れてしまいます。**1 本ずつ完結させます。**

#### 確かめる候補も、言葉ごとに

何本も辿ると、どの言葉から出た名前か分からなくなっていました。
行ごとに開くので、**その言葉で何を持っていないか**がその場で分かります。

★これは「買え」という意味ではありません。**見て「持っていないな」と
気づいてもらうため**のものです。買う・落とす仕掛けは置いていません。
そして 3 種類が混ざることも変わりません（本当に持っていない／書かれ方が違う／
AI の思い違い）。だから「確かめる候補」という名前のままです。

#### 一本を組むのも、言葉ごとに

押すと、**その言葉だけを 3 カラムで選んでから**組みます。
何から組んだのかが画面に残るので、あとから確かめられます。

★0.13.0 で「響きのところに組むボタンは置かない」と決めたことがあります。
あのときは**押すつもりのないボタンが 2 つ**あって、事故が起きたからでした。
いまは種類が 1 つで、行ごとに並ぶだけです。**どれを押しても同じことが起きます。**

### 0.36.1 で直したこと（0.43.1 にも入っています）

**★README の約束に、誤りがありました。**

「演者の一覧も送りません」と書いていましたが、**🌱 言葉から辿るときは送っています。**
この人の演者名を最大 800 組、曲数つきで送っていました。
0.35.0 で 🎧 のほうに「送りません」と書いたとき、🌱 のことを見落としました。

**送るものの表を、機能ごとに分けて書き直しました。**

```
🎧 気分から一本   ジャンル名／年／気分の文／候補 200 曲
                  ★演者の一覧は送らない

🌱 言葉から辿る   打ち込んだ言葉／★演者名 最大 800 組／蔵書の大きさ
```

★**なぜ 🌱 は送るのか。** 「この人の蔵書に合わせて辿る」ためです。
送らないと、手元と無関係な名前ばかり返ります。**必要だから送っています。**
ただ、書いていなければ約束を破ったのと同じです。

#### 辿る段で「王道」が効いていませんでした

> helmet を王道でやったのですが、王道以外がいっぱいひっかりました。

★頼み文が**数を最優先**にしていました。

```
・★できるだけ多くの、別々のバンドを挙げてください。数がほしいところです
　上の一覧にある名前を選んで構いません。むしろ歓迎します
```

その一覧は**この人の蔵書**（最大 800 組）です。ハードコアの蔵書を見せて
「ここから数多く」と頼めば、返るのはハードコアになります。
強度の「ど真ん中を選べ」が、**数の指示に負けていました。**

いまは、つまみで先後が入れ替わります。

```
王道      数より近さが先。少なくてよい。一覧に寄せない
やや王道  近さを優先。数は二の次
ふつう〜  できるだけ多く（これまでどおり）
```

★王道側では「一覧はこの人の蔵書です。**そこに寄せて選ぶと、軸から外れます**」
とまで言っています。

#### ★木は作り直しになります

辿った木は `resonance.json` に貯まります。**すでに辿ったものは、そのままです。**
つまみを変えたら、**同じ言葉でもう一度「辿る」を押してください。**
深さの重み（0.36.0）は並べ替えるだけですが、こちらは**木そのものが変わります。**

### 0.36.0 で変わったこと（0.43.1 にも入っています）

**響きの「深さの重み」を、つまみ（王道↔外す）に繋ぎました。**

> 響きを知覚を濃くするためにつまみに繋げてください。

★これまでは**固定で「深いほど重い」**でした（1〜4 倍）。
遠い当たりのほうが発見として面白い、という当初の指示によるものです。
ただ「軸のそばを濃くしたい」ときは、これが逆に働きます。

| つまみ | 深さ 0 | 1 | 2 | 3 |
|---|---:|---:|---:|---:|
| 王道 | **4** | 3 | 2 | 1 |
| やや王道 | 3 | 2.5 | 2 | 1.5 |
| ふつう | 1 | 1 | 1 | 1 |
| やや外す | 1 | 2 | 3 | **4** |
| 外す | 1 | 3 | 5 | **7** |

★**既定の動きが変わります。**「ふつう」は深さで差を付けません。
これまでどおりが良ければ、**つまみを 1 つ右（やや外す）**へ。

#### なぜ入れたか

「Helmet みたいな」で組んだ一本が広がりすぎる、という話の続きです。
響きは多段で辿れるので「2 段目」に使えますが、**深いほど重い**ので
辿るほど軸から離れます。つまみに繋げば、そこを行き来できます。

つまみを動かすと、響きタブの並びもその場で並べ直します。

★控えは持たず、**つまみから直に読みます。**
控えを持つと「動かしたのに古い値のまま」という食い違いが起きるためです。

### 0.35.3 で変わったこと（0.43.1 にも入っています）

**「なぜそうなったか」を画面に出すようにしました。**

> まだ拡大解釈な感じです。
> こちらの環境や設定が悪いかもしれないので、そこも考慮して考えてもらえると助かります。

#### ★環境のせいでした。ただし「悪い」のではありません

つまみを王道に振っても、Godflesh や Kyuss や Botch が出てくる。
**手元のフォルダ名を数えたら、理由がはっきりしました。**

```
Helmet と同じ棚の 26 組   手元に 5 組だけ（19%）
  持っていない: Failure / Hum / Shiner / Chavez / Jawbox /
                Unsane / The Jesus Lizard / Prong / Clutch …

一本に入っていた 18 組     手元に 18 組（100%）
```

★**AI は正しい名前を挙げていて、突き合わせで落ちていました。**
この蔵書には「Helmet の車線」がほとんど無く、近いところで持っているのは重い側だけです。
だからそちらが残る ―― 挙げた 80 組のうち、当たったのが 25 組でした。

**つまみをどう振っても、持っていないものは出せません。**
ならば、そう言うべきです。黙って持っている側だけ出すから「拡大解釈」に見えます。

#### 出すようにしたもの

```
※ AI は 80 組挙げましたが、手元にあったのは 25 組でした
  （Failure / Hum / Shiner / Chavez などは持っていません）
```

★持っていない名前が出るのは、**響きタブの「確かめる候補」と同じ考え方**です。
買い物リストではありません。「この車線は手元が薄い」と分かるためのものです。

#### ★間違った理由を出していました

曲数が減ったとき、いつも「候補が足りない」と言っていました。
演者で絞っているときの本当の理由は「**組が足りない**」です。

```
前  （候補が 200 曲しかないので 25 曲にしました）
後  （この気分で手元にあったのが 25 組なので、25 曲にしました）
```

### 0.35.2 で直したこと（0.43.1 にも入っています）

**つまみ（王道↔外す）が、演者を挙げる段に届いていませんでした。**

> 拡大解釈としてはかなり好きなセレクトなんですが、
> 僕としてはロックの中から選んでほしかったなという感じです。

0.35.1 で演者の道は通りました。「Helmet みたいなバキバキのオルタナロック」に対して、
Helmet / Quicksand / Fudge Tunnel / Godflesh / Biohazard / Kyuss / Botch / Converge…
と 22 組。**近いけれど、ロックの外まで広がっていました。**

★つまみは「王道」に振ってあり、拡大解釈も切ってありました。それなのに広がった。
調べたら、**強度が演者を挙げる段に渡っていませんでした。**
一本を組む段にしか効いていなかったのです。

いまは段ごとに、どこまで広げるかを言います。

```
王道      その音そのもののど真ん中に留める。近い界隈へ広げない
ふつう    その音を軸に、近い界隈まで
外す      ジャンルも時代も跨いで、気配だけが通じるものを
```

#### ★また同じ罠を踏みました

新しい文を **強度の段の中で `言()` に通してしまい**、読み込みのときに固めていました。
0.32.0 でも同じことをしています。段は日本語のまま持ち、**使うときに訳す**のが作法です。

見張り（`check-knob.js` の `[強度→演者]`）を足して、
**段の中で `言()` を呼んでいないこと**まで見るようにしました。

### 0.35.1 で直したこと（0.43.1 にも入っています）

**0.35.0 で入れた手当てを、その日のうちに撤回しました。**

> helmet みたいなバキバキのオルタナロックで試しましたが、
> 90s hardcore と noise を検索して結果このようになりました

★**「Rock」「Alternative」のような大まかな名前は避けろ、が裏目に出ました。**
Helmet にいちばん近い札を封じられ、AI はより遠い名前へ逃げました。
受け皿の問題は、**ジャンルの層では直りません。** 禁じる行を外しました。

★**演者の道が働いていませんでした。** 「並べる曲数ぶん（30 組）そろったときだけ」
という敷居が厳しすぎて、毎回ジャンルに落ちていました。

いまは **6 組でも当たれば演者で絞ります。** 足りないぶんは**一本を短くします**
（同じ演者を並べるより、短いほうがいい）。短くしたことは画面に出ます。

★組の数は、頼む曲数の**上限としてだけ**画面から渡します。増やす方向には効きません。
つまみは今までどおり本体で読みます。

### 0.35.0 で変わったこと（0.43.1 にも入っています）

**気分から「演者」で絞れるようにしました。ジャンル名を通しません。**

本人の指摘（2026-08-31）:

> ロックって音楽ジャンルとしてもいいますが、音楽形態としてもいいますよね？
> メタルもグラインドコアも極論ではロックだから、
> 僕のライブラリのほとんどがロックになっちゃう、みたいな感じです。

#### ★そのとおりでした

「Helmet みたいなバキバキのオルタナロック」と書いたのに、
Napalm Death や So Hideous が入ってくる ―― という報告から調べました。

**ファイルのタグを読んだら、こう書いてありました。**

```
Napalm Death / Live Corruption / Malicious Intent      "Rock"
So Hideous   / None But A Pure Heart / Emerald Pearl   "Alternative"
I Promised The World / Fear of the Fall                "Rock"
Blitz        / Voice of a Generation                   "Rock"
```

**絞り込みは 1 曲も漏れていませんでした。** タグがそう書いてあったのです。

なぜそうなるか。演者 172 組を抜いてタグを読むと:

```
"Rock" か "Alternative" の組     7 / 172（4%）
  A Perfect Circle / Bauhaus / Citizen / Godspeed You! Black Emperor
  Puffy / Wipers / Asbesto
```

★**この蔵書では「Rock」は受け皿でした。** iTunes から入ったぶんだけが
大まかな名前になっていて、中身はばらばらです。
同じ Napalm Death でも、別フォルダの 302 曲は "Grindcore" と正しく付いています。

そして本人の言うとおり、**広い意味のロックはこの蔵書の 9 割方**を占めます。
「ロック」で絞るのは、絞ったことになりません。

#### 直しどころは、定義ではありませんでした

「ロックをちゃんと定義したほうがいいか」と訊かれましたが、**しません。**
正しく定義すると 9 割が該当してしまい、絞り込みとして働かないからです。

★本当の問題は、**いちばん情報量が多い「Helmet みたいな」を捨てていた**ことです。
「バキバキの」も「〜みたいな」も、ジャンル名という器には入りません。
器に入れた時点で、残るのは「Rock」という、この蔵書ではほぼ無意味な札だけでした。

#### だから、演者で絞ります

気分を書くと、AI は**ジャンルに加えて「近い演者を 80 組」**挙げます。
こちらで手元と突き合わせ、**そろっていれば演者で絞ります**（足りなければ今までどおりジャンル）。

★**演者の一覧は送りません。** AI が自分の知識で挙げた名前を、
こちら側で照らすだけです。外に出るものは今までと変わりません。

★突き合わせは**完全一致**（大文字小文字だけ無視）。部分一致にすると
短い名前が別の演者を巻き込みます ―― 響きタブと同じ考え方です。

★**挙げたが手元に無かった演者は「無かったもの」に出しません。**
80 組挙げさせて持っていないのが普通なので、毎回 60 個並んでしまいます。

あわせて、ジャンルを選ぶときに
**「Rock」「Alternative」のような大まかな名前は避ける**よう頼み文に足しました。

### 0.34.1 で直したこと（0.43.1 にも入っています）

**同じバンド・同じアルバムからの選曲が増えていたのを直しました。**

> 同じバンドや同じバンドの同じアルバムからの選曲が多くなりました。
> 同じバンドはできるだけ避けるようにしたいです。

★0.34.0 で 1 組から 3 曲渡すようにしたぶん、**AI が同じ組から 2 曲・3 曲と
拾ってしまう余地を作ってしまいました。** 直しどころは 2 つありました。

#### ① 候補表が散らばっていました

引くのは巡ごと（1 周目に全組から 1 曲、2 周目にもう 1 曲…）なので、
そのまま並べると**同じ演者が 67 行おきに散らばります。**
AI からは「同じ組の 3 曲」ではなく「別々の良い曲 3 つ」に見えていました。

演者ごとにまとめてから番号を振るようにしました。
頼み文でも「**1 組から選ぶのは 1 曲だけ**」と言っています。

#### ② 返ってきた並びを、鵜呑みにしていました

★頼み文に書くだけでは足りません。**こちらで落とします。**
このアプリは前から「候補に無い番号」「同じ番号が 2 回」を落としていました。
そこに「**同じ演者・同じ盤が 2 回**」を足しました。

落とした数は画面に出します。黙って短くはしません。

★**避けられないときは避けません。** 候補に 5 組しかいないのに 30 曲を頼めば、
重なるほかありません。そのときは落としません ―― シャッフルと同じ考え方です。

#### 途中で見つかったこと

★検査が、関数の終わりを `return 出;` という**中の名前で探していました。**
0.34.1 で `return 並べた;` に変わったとたん、切り出しが途中で切れて落ちました。
桁 0 の閉じ括弧で探す形に直しました。

わざと **①重なりを落とさない ②組が足りなくても落とす ③演者ごとにまとめない**
の 3 通りを試し、すべて捕まえることを確かめました。

### 0.34.0 で変わったこと（0.43.1 にも入っています）

**シャッフルと、一本を組むときの選曲を分けました。**

本人の言葉（2026-08-31）:

> バンドのチョイスはすごく良くなったんですが、選曲が微妙だなと思いました。
> プレイリストの良さは、そのバンドのコアとなる曲と、
> 時々スパイスとなる曲の組み合わせの良さだと思うので

#### ★思っていた原因ではありませんでした

「再生回数が多い曲を選ばないでほしい」と頼んだせいだ、という見立てでしたが、
数えると **86,044 曲のうち、再生回数が付いているのは 8 曲だけ**でした。
重み `1/(n+1)` は、ほぼ全曲で 1 です。**もともと効いていませんでした。**

本当の原因は、**演者 1 組につき 1 曲しか渡していなかった**ことです。
候補 200 曲＝200 組×1 曲なので、**AI にはどの曲を出すか選べません。**
ディスコグラフィから無作為に引いた 1 曲を、そのまま並べるしかありませんでした。

#### 組を絞って、1 組から何曲か渡します

どの曲がその演者の代表曲かは、**AI のほうがよく知っています。** そこを任せます。

```
目盛1  50/10 → 2 曲 ×  25 組     目盛4 400/50 → 3 曲 × 134 組
目盛2 100/20 → 2 曲 ×  50 組     目盛5 800/80 → 4 曲 × 200 組
目盛3 200/30 → 3 曲 ×  67 組
```

組の数は、**並べる曲数の 2 倍以上**を残してあります（そこは AI に選ばせたいので）。
★幅を広げると、組の数**と** 1 組あたりの曲数の両方が増えます。
つまみが「バンドの幅」だけでなく、**選曲の質にも効く**ようになりました。

#### ★名曲が置かれやすい場所

本人の見立てです。

> ・1曲目：しかし、インストのイントロの可能性も高い
> ・2曲目：現代では2曲目にオープニング的な名曲を配置することが多い
> ・アルバムの真ん中：レコードのA面の終わりやB面の1曲目として印象的な曲
> ・最後の曲：とんでもない名曲もあるけど、冗長な曲もあり博打度が高い

**測ったら、そのとおりでした。** 題が intro / outro / interlude … の曲が、どこに多いか
（この蔵書、番号の通った盤 6,743 枚）:

| 場所 | つなぎ率 | その他の何倍 |
|---|---:|---:|
| 1 曲目 | 6.78% | **10.5 倍** |
| 2 曲目 | **0.31%** | 0.5 倍 |
| 真ん中 | 0.89% | 1.4 倍 |
| 最後 | 2.04% | 3.1 倍 |
| その他 | 0.65% | 1.0 倍 |

**2 曲目は、盤の中でいちばんきれいな場所**でした。最後は博打、も数字どおりです。

重みは、この 3 つを足しています。

```
場所            2 曲目・真ん中 3 点 ／ 1 曲目 2 点 ／ 最後・その他 1 点
同じ題が 2 枚以上に  ＋3 点   ベスト盤・ディスコグラフィ盤に  ＋2 点
```

★「同じ題が 2 枚以上に」は、**蔵書そのものが投票している**ということです
（2 枚以上持っている演者 1,730 組のうち 680 組・39% にあります）。

★**最後の 1 杯だけ、重みを外します。** これがスパイスです。
核ばかり並べると、知っている曲だけの一本になります。

#### 効き目

見立てた範囲 3,653 曲・232 組で、候補 200 曲を 30 回作った平均です。

| | 前 | 後 |
|---|---|---|
| 組 | 200 組 × 1 曲 | **67 組 × 3 曲** |
| 2 曲目 | 10% | 12% |
| 真ん中 | 22% | **28%** |
| 最後（博打） | 16% | **12%** |
| その他 | 42% | 37% |

★**位置の重みは、味付け程度です。** 効き目の大半は「AI に選ばせる」ほうにあります。

#### 途中で踏んだ罠

★**重みは 1 を超えられませんでした。**
`次を選ぶ()` の重みは `1/(n+1)` なので、どう頑張っても最大 1 です。
素朴に `n = 1/w - 1` と置くと、w が 1 を超えた瞬間に n が負になり、
`Math.max(0, …)` で 0 に潰れて**全曲が同じ重みになります。**

実際そうなっていて、**位置の重みがまったく効いていませんでした**
（前後で 2 曲目 10%→11%。動いていない）。測っていなければ気づけませんでした。
いちばん重い曲を 1 に合わせてから割り当てる形に直しました。

見張り（`check-knob.js` の `[核]`）を足して、
**①重みを潰す ②1 組 1 曲に戻す ③候補に再生回数を混ぜる** の 3 通りを
わざと試し、すべて捕まえることを確かめました。

### 0.33.1 で直したこと（0.43.1 にも入っています）

**画面のいちばん下の 3 つが、英語にしても日本語のまま残っていました。**

```
再スキャン
🏷 タグ無しも出す
↩ 外した 2,349 曲を戻す
```

どれも「そのボタンの中身が変わったとき」にだけ書き換える作りで、
**画面を描き直すときには呼ばれていませんでした。**
言葉を変えるのは画面ぜんぶが変わることなので、描き直しから呼ぶようにしました。

#### なぜ 0.33.0 の検査をすり抜けたか

★検査は**仮の置き場**で開くので、ライブラリが空です。
すると「外したものを戻す」は隠れたままで、日本語が残っていても気づけません。

そこで検査のほうを直しました。
言葉を変える前に、下のボタンを**本人の画面と同じ形にしておく**ようにしています。
わざと元の作りに戻して、検査が捕まえることを確かめました。

### 0.33.0 で増えたこと（0.43.1 にも入っています）

**画面の文言も英語になりました。これで英語訳がひととおり揃いました。**

```
訳す対象（註釈を除く）  669 個

0.31.0   17 個（AI の欄）
0.32.0  214 個（＋ AI への頼み文）
0.33.0  437 個（＋ 画面の文言すべて）
```

#### 訳してはいけないものがある

日本語の文字列を片端から訳すのは**間違いです。**
カラムのタブの名前やデータの鍵など、**比べるための文字列**が混ざっていて、
それを訳すと比較が壊れて機能が死にます。

だから「画面に出る場所」から拾いました。
それでも 3 か所、**読み込みのときに一度だけ組み立てられるもの**があり、
そこで訳すとその時点の言葉で固まってしまいます。
日本語のまま持ち、**読むたびに訳す**形に直しました。

```
カラムタブの列   … 列の見出し
繰り返しの表示   … 🔁 しない / 1曲 / 全体
強度の段        … 0.32.0 で同じ罠を踏んだところ
```

#### 曲名を訳してしまわないために

画面の土台（index.html）の文言は、画面じゅうを見て回って訳します。
ただし**そのやり方には危険があります。** 「再生」という名前の曲があれば、
曲名まで訳してしまいます。

そこで、**台本が動き出した時点で見えているものだけ**を覚えておき、
言葉が変わるたびに、覚えた元の日本語から訳し直すようにしました。
あとから作られたもの（曲の一覧、演者の名前）には、いっさい触りません。

#### 開いてみて見つかった不具合

**genre.js と naoshi.js が、どちらも大域に同じ名前を作っていました。**
画面では `<script>` として並べて読み込むので、
**2 本目（naoshi.js）が丸ごと読み込まれていませんでした**（0.32.0 に入っていました）。

幸い、画面側は naoshi.js の中身を呼んでいなかったので、見た目の症状は出ていません。
ただ、起動のたびに黙って読み込みに失敗していました。

★node からは別々に読み込むので、**それまでのどの検査にも掛かりませんでした。**
開いて見るまで分からない類です。

#### 見張りを 2 本足しました

```
check-en.js       英語にして、本物の画面を開いて読む
check-lang.js [7] 並べて読み込む台本が、名前をぶつけていないか
```

check-en.js は**仮の置き場**を使うので、音蔵を開いたままでも走ります。
わざと名前をぶつけて [7] が捕まえることを確かめました。

### 0.32.0 で増えたこと（0.43.1 にも入っています）

**AI への頼み文を英語にしました。これで AI も英語で返します。**

英語にすると、5 本の頼み文がまるごと英語になります。

```
You are a DJ. From the songs this person owns, build **one set, in playing order**.

■ Rules
- Choose **30 songs** from the list and put them **in playing order**
- ★**One song per artist.** One song per album as well
  (there are 200 artists in the list, so all 30 can be different)
- ★Do not pick intros, outros or interludes.
- Write the note in English, one short sentence: **why this song, in this position**
```

★最後の 2 行が要です。**「英語で返せ」と頼み文の中で言っています。**
画面だけ英語にして頼み文が日本語のままだと、返事は日本語で返ってきます。

#### 途中で見つかったこと

**文脈の強度の言い回しが、切り替わりませんでした。**
段は読み込みのときに一度だけ組み立てられるので、
そこで訳すと**そのときの言葉で固まってしまう**からです。
段は日本語のまま持ち、**読むたびに訳す**形に直しました。

**英語には単数・複数が要ります。** 「1 songs」が出ていたので、
訳の値に `|` を書けば単数／複数を分けられるようにしました。

```
'{n}曲': '{n} song|{n} songs'   →  1 song ／ 12 songs
```

#### いまどこまで

```
訳す対象（註釈を除く）  669 個

0.31.0   17 個（AI の欄）
0.32.0  214 個（＋ AI への頼み文 127 個ほか）
```

残りは画面の文言です。次はそこへ進み、最後に英語版の README を作ります。

### 0.31.0 で増えたこと（0.43.1 にも入っています）

**英語対応を始めました。まず土台と、AI の欄です。**

下の欄に **`🌐 自動（日本語）`** が出ます。押すと 自動 → 日本語 → English と回ります。
★**OS の言葉を見て自動で決めたうえで、手でも切り替えられます**（ご指定どおり）。

#### ★訳の鍵を、日本語そのものにしました

```
言('一本を組む')                 日本語ならそのまま ／ 英語なら Build a set
言('{n} 曲', { n: 30 })         30 曲 ／ 30 songs
```

理由は、**検査が 16 本あり、その多くが日本語の文字列を当てにしている**からです。
鍵を日本語にすれば、ソースにその文字列が残るので**既存の検査がそのまま通ります。**
別の ID を振ると、16 本ぜんぶ書き換えることになります。

★もうひとつ大事なのは、**訳が無い文は日本語のまま出る**ことです。
途中まで訳した状態でも壊れません。少しずつ進められます。

#### いまどこまで

```
訳す対象（註釈を除く）  669 個 ／ 17,598 文字
  画面                   436
  AI への頼み文          170
  本体・その他            63

0.31.0 で通したもの      17 個（AI の欄まわり）
```

実際に窓を開けて、英語になることを確かめています。

```
Build a set / Trace / Re-group genre names / Fill in missing genres /
Undo grouping / Show my edits / Discard my edits (1) / 🛠 Tidy up ▼ /
🌀 Wide reading: off / ⏹ Stop

Narrow | Wide | 10 songs | 80 songs | Classic | Off-centre
```

次は AI への頼み文です。塊ごと差し替えられるので、実は画面より楽です。
そのあと画面の残り、最後に英語版の README を作ります。

### 0.30.1 で直したこと（0.43.1 にも入っています）

**つまみの名前が 2 行に折り返していたのを直しました。**

本人の指摘（画面の写真つき）:

> スライダーの名前の表示をなんとかしたいなと思いました。
> 狭い↔広い、10曲↔80曲、王道↔外す、みたいな表現だと使い方わからないですかね？
> 表示をシンプルにしたいです。

幅が足りないのに名前を置いていたので、「対象／の幅」「文脈の強／度」と
2 行に折り返していました。**名前をやめて、両端を出します。**

```
これまで   対象の幅 ―●― 広い ／ 選出の量 ―●― 80 曲 ／ 文脈の強度 ―●― 外す
これから   狭い ―●― 広い ／ 10 曲 ―●― 80 曲 ／ 王道 ―●― 外す
```

★片方だけを今の値にすると「狭い ―●― やや広い」となり、**範囲に見えて紛らわしい**ので、
両端にしました。つまみの位置が、そのまま今どこかを表します。

★いまの値は、下の説きがすでに数で出しています。

```
候補 400 曲 ／ 辿る名前 55 個 ／ ジャンル 8 まで　文脈は「外す」　→ 80 曲の一本（目安 16.4 円）
```

★名前（対象の幅・選出の量・文脈の強度）は、つまみに触れると出ます。

### 0.30.0 で変わったこと（0.43.1 にも入っています）

**拡大解釈を、強度のスライダーから外して独立した入／切にしました。**

本人の希望:

> 拡大解釈機能ですが、スライダーから外して独立したオンオフボタンにしたいです。
> というのも拡大解釈って広い幅の中から王道的な者同士であったり
> 外した者同士であったり、と種類があるので
> スライダーの外しの部分だけでは条件を満たせないと思いました。
> 王道の拡大解釈が意外と面白いと思ったので、独立したボタンがいいのかなと思いました。

★言われてみれば、これは**別の軸**でした。

```
文脈の強度  … 何を選ぶか（王道のもの ↔ 端のもの）
拡大解釈    … どこまで広げるか（近くで留まる ↔ 遠くまで飛ぶ）
```

0.27.0 では後者を前者に相乗りさせたので、
**「王道のものを、遠くまで広げて選ぶ」が言えませんでした。**

#### 分けたので、4 通りぜんぶ言えます

| | 拡大解釈 切 | 拡大解釈 入 |
|---|---|---|
| **王道** | 王道のものを、近くで | ★**王道のものを、遠くまで広げて** |
| **ふつう** | 混ぜて、近くで | 混ぜて、遠くまで |
| **外す** | 端のものを、近くで | 端のものを、遠くまで広げて |

★**入にしたときの「飛んだ先での選び方」は、文脈の強度が決めます。**

```
王道 × 入   飛んだ先でも、その界隈の王道を選んでください
           遠くへは行くが、行った先では代表格を選ぶ ―― という広げ方

外す × 入   飛んだ先でも、端のほうを選んでください
           遠くへ行ったうえで、そこでも意外なものを選ぶ ―― という広げ方
```

つまみの行に **`🌀 拡大解釈 切`** が出ます。押すと入になり、色が付きます。
既定は切です。入のときも、飛び方に筋を通すこと
（なぜそこへ行ったかを「ひとこと」で言えること）は変えていません。

### 0.29.0 で増えたこと（0.43.1 にも入っています）

**AI の生成を、途中で止められるようになりました。**

本人の希望:

> AIに指示を出した後、生成を途中で止めることってできますか？
> 結構これができないのが地味に不便なので。

走っている間だけ **`⏹ 止める`** が出ます。押せばその場で切れます。

★止めたときは「だめでした」ではなく **「止めました（何も作っていません）」** と出ます。
自分で押したのに壊れたように見えるのが、いちばん困るので。

#### 効くのは 5 つすべて

| | |
|---|---|
| 🎧 一本を組む | 気分から絞るところ・組むところ、どちらも |
| 🌱 辿る | 言葉から辿るところ |
| 🏷 ジャンル名をまとめる | |
| ジャンル名無しを埋める | AI に訊く段だけ（手元で決まるぶんは一瞬です） |

#### 実測

遅い窓口を立てて確かめました（★本物の AI には繋いでいません）。

```
止めなければ   30 秒かかる呼び出し
1.5 秒後に止める → ★1.7 秒で返った
```

★**そこまでに使ったぶんは請求されます。** 止めても「ただになる」わけではなく、
**途中で切るだけ**です。長い生成を待たずに済む、というものです。

### 0.28.1 で直したこと（0.43.1 にも入っています）

**手でジャンルを直しても、AI の手直しが上書きし続けていました。**

本人の報告:

> ジャンルをまとめたタブの中でタグ編集（ジャンルの編集）をすると
> ジャンルをまとめたタブ内で反映されなかったので反映したいです。
> オリジナルのタブの方へは反映を確認しました。
> （あ、ジャンル名無しを変更しました）

★**「ジャンル名無し」の曲には、AI で埋めた手直しが載っています。**
手直しは「見るときに重ねる層」なので、その曲のタグを手で直しても、
**重ねる層のほうが勝ち続けていました。**
mp3 も覚え書きも新しい値になっているのに、画面では古い値のまま ―― です。

本体を 3 回立ち上げて確かめた、直す前の姿:

```
AI の手直しで  ジャンル名無し → Punk
手で直すと      Punk → Hardcore
画面に出るのは  ★Punk のまま
```

**手で直したほうが新しく、意図もはっきりしています。手を勝たせました。**
タグを書いたら、その曲のその欄の手直しは外します。

```
AI の手直しのあと         Punk
手で Hardcore に直した     Hardcore
★立ち上げ直したあと        Hardcore（手の直しが残る）
```

★外すのは**書いた欄だけ**です。
アーティストだけ直したのにジャンルの手直しまで消えるのは、
頼んでいないことをしたことになるので。

### 0.28.0 で増えたこと（0.43.1 にも入っています）

**打った文の履歴が残るようになりました。**

本人の希望:

> 一回使ったら入力欄の文字は消えてほしいと思う反面、
> 同じ条件でどんなのできるかな？と試そうとする自分もいて
> イチイチ文字が消えるのが面倒くさいと思います。
> 入力欄の履歴が残って入力欄をクリックするとしたに履歴が表示される、
> みたいな機能があればいいのかな？と思ったんですが、どうですか？
> ちなみに、履歴は消したいとも思うので履歴を消すなにかもほしいのですが。

**入力欄をクリックすると、下に履歴が出ます。** 押せばその文が入ります。
🎧 の気分と 🌱 の言葉は、別々に覚えます。

★**組めたとき・辿れたときだけ**覚えます。だめだった文は残しません。
★同じ文を何度試しても増えません。**上に来るだけ**です。
★20 件までで、古いものから落ちていきます。

#### 消し方は 2 通り

| | |
|---|---|
| 1 件だけ | その行の右の `✕` |
| まとめて | 一番下の「この履歴を全部消す（N 件）」 |

★履歴は `履歴.json` という**別のファイル**に置いてあります。
消しても、ほかの設定を巻き込みません。

#### 「欄が消える」のはそのままです

組めたら欄は空になります（0.15.2 からの動き）。
**履歴があれば戻すのは 1 クリック**なので、
「消えてほしい」と「また使いたい」が両方かないます。

#### ★設定が書けたかを確かめるようにしました

これは別件の用心です。実地で、`settings.json` が 5 日間まったく
更新されていないのに、同じ置き場の `手直し.json` は書けている、
という状態が見つかりました。コードは同じ形で、原因は突き止められていません。

**分からないものを黙らせないため**、設定を書いたあとに読み返して、
中身が合わなければ画面に出すようにしました。

### 0.27.1 で直したこと（0.43.1 にも入っています）

**「どうやってリセットするんだ？」を直しました。**

本人の報告:

> 「ジャンル/アーティスト/アルバム」タブと自分で生成した「ジャンルタブ」があり、
> プレイリストの「ライブラリ」ボタンもあって「どうやってリセットするんだ？」となります。
> プレイリストが生成されたらジャンルの選択は一度リセットされることと、
> 上のカラムのタブを押したらプレイリストは閉じて
> ライブラリのリストが表示されたらいいのかなと思いました。

★タブが増えたぶん、外す場所も増えていました。
**隠れているタブの絞り込みも効く作り**なので、見えている列を「すべて」に
戻しても、まだ絞られていることがあります。それが分かりにくさの正体でした。

直したのは 3 つです。

**① 一本を組んだら、絞り込みを外します。**
絞り込みは「この一本を組むための指定」でした。組んだら役目は終わりです。

**② カラムのタブを押したら、再生リストを閉じてライブラリに戻ります。**
これで「カラムを触る＝ライブラリを見ている」に揃います。
前は、再生リストを開いたままカラムを触れてしまい、
絞ったのに一覧が変わらない（開いた一本が出ている）ことがありました。

**③ 何か絞っているときだけ、`✕ 絞りを外す（3）` が出ます。**
★数には**隠れているタブのぶんも入ります。** 一度押せば全部外れます。

★外し方は 1 か所にまとめました。絞る欄を足したときに
「1 つだけ外れない」が起きないようにしてあります
（同じ形の事故を、0.26.1 のボタンで一度やっています）。

### 0.27.0 で変わったこと（0.43.1 にも入っています）

**① ボタンを「🛠 整える」にたたみました。**

> ボタンが多くて AI の指示欄をあっぱくしてるのでべつのどこかにまとめたいです。
> 一度使ったらあまり使わない機能なので

表に出るのは **🎧 一本を組む** と **🌱 辿る** だけになりました。
ジャンル名をまとめる・ジャンル名無しを埋める・手直しの出し入れは、
右端の **🛠 整える ▼** の中です。既定では閉じています。

**② イントロを選ばなくなりました。**

> 高確率でアルバム収録1曲めのインストを1曲目に選ぶことです。

原因は頼み文のこの 1 行でした。

```
・ただ合う曲を並べるのではなく、流れを作ってください。
　入りは軽く、中盤で上げて、終わりは落ち着かせる ―― といった起伏を意識する
```

**「入りは軽く」と言われた AI は、素直にイントロを頭へ置きます。**

候補の側でも落とすようにしました。**題名が丸ごと Intro / Outro /
Interlude などのときだけ**です。実測で 602 曲（0.71%）。

```
落ちるもの  Intro 417／Outro 104／Interlude 44／Epilogue 11／Prelude 7 …
            ★そのうち 61% は盤の 1 曲目

落ちないもの Prelude To Extinction／Intro/Say Goodbye／Overture 1948
```

★長さでは切っていません。この蔵書は **60 秒未満が 14.2%（12,176 曲）**あり、
グラインドやパワーヴァイオレンスでは 30 秒が**ふつうの曲**だからです。

★一覧からもくじからも外していません。AI に組ませるときの候補から外すだけです。

**③ 「拡大解釈」を、文脈の強度に預けました。**

> プレイリストの後半になるとセレクトするバンドがものすごい拡大解釈になることです。
> これ、めちゃくちゃ面白い機能なので入れたいんですが、
> 王道セレクトでこれをされるとつらいところがあるなと思いました。

②と**同じ 1 行**が原因でした。「起伏を作れ」と言われた AI は、
後半で遠くへ手を伸ばします。しかも強度に関係なく、いつもそう言っていました。

消さずに、強度で切り替えるようにしました。

| 強度 | 後半の広がり方 |
|---|---|
| 王道 | **最後まで同じ文脈を保つ。**1 曲目と最後が地続き |
| やや王道 | 文脈は保つ。軽い起伏だけ |
| ふつう | 起伏をつける。少しだけ離れてよい |
| やや外す | 前半で足場を作り、後半で広げる |
| 外す | ★**後半は遠くの文脈まで飛んでよい。**「そう来たか」と思うところまで |

★飛び方には筋を通させます。なぜそこへ行ったかを「ひとこと」で言えること、
という条件を付けてあります。

### 0.26.1 で直したこと（0.43.1 にも入っています）

**押したあと、一部のボタンがグレーのまま戻らなくなっていました。**

本人の報告:

> ジャンル名をまとめるをやったあと、ジャンル名無しを埋めると
> まとめをやめるのボタンがグレーになって使えなくなりました。
> 立ち上げ直したらボタンが白に戻りました。

原因は、**止める側と戻す側で一覧が別々に書いてあった**ことです。

```
止める … ボタン 9 つ ＋ つまみ 3 つ をまとめて止める（1 か所）
戻す   … ★id を直書きした一覧が 4 か所。どれも中身が違った
```

ボタンを足すたびに 4 か所を直す作りだったので、0.26.0 で足した 4 つ
（まとめをやめる・ジャンル名無しを埋める・手直しを見る・手直しを捨てる）が
どの一覧にも入っていませんでした。

AI の欄は「形が変わったときだけ」作り直すので、画面を描き直しても戻りません。
だから**立ち上げ直すまで直らなかった**わけです。

★一覧を 1 か所にまとめ、戻すのも同じ一覧を使うようにしました。
ボタンを足すときは、そこに足すだけで止めるのも戻るのも揃います。

### 0.26.0 で増えたこと（0.43.1 にも入っています）

**① 直したことを、消せる別ファイルに残すようにしました。**

本人の希望:

> ジャンル名をまとめるや自分でタグを直しても立ち上げ直すともとに戻るので
> どこかに記録をセーブして、そのセーブデータを削除や直すことで
> 元通りにすることはできないでしょうか？

`手直し.json` という小さなファイルに残します。
**このファイルを消せば、完全に元通りになります。**

```
{
  "曲": {
    "E:\\iTunes Music\\Balmora\\in oblivion\\01.mp3": {
      "genre": "Metal Core",
      "元": { "genre": "ジャンル名無し" },
      "訳": "AI: 盤名から"
    }
  },
  "直した日": "2026-08-30"
}
```

★**元の値も一緒に残します。** 何を何に変えたのか分かるので、
1 件だけ手で戻すこともできます。

★覚え書き（`library-cache.json`・32 MB）とは**別のファイル**にしました。
覚え書きは走査が作り直すものなので、手で直したものを混ぜると、
走査のたびに消えるかどうかが読めなくなります。
それに 32 MB の中では、開いて直すことすらできません。

欄に **「手直しを見る」**（置き場を開く）と **「手直しを捨てる」** が出ます。
手直しで入ったジャンルの曲は、一覧で曲名のうしろに `✚` が付きます。

**② ジャンルの付いていない曲に、ジャンルを入れられるようにしました。**

> ジャンル名無しのデータがたくさんあってそれをいちいち振り分けるのが大変だから
> AI の力でジャンル名無しのデータを他のジャンルに振り分けられないでしょうか？
> 多少の間違いはありにします（振り分けられないことのほうが面倒なので）。

★測ったら、**半分以上は AI に訊く必要がありませんでした。**

```
ジャンル未定        2,601 曲（蔵書の 3.0%）

  手元で決まる      1,525 曲（59%）★同じ演者の他の曲に付いているジャンル
  AI に訊く         1,076 曲 ／ 160 組 ＝ 1,755 トークン
```

`Leatherface` の 2 曲は、同じ演者の他の **352 曲**が Punk なので、
推測ですらありません。**まず手元で決めて、決まらないぶんだけ AI に訊きます。**
安く、速く、確かになります。

見てもらう画面では、この 2 つを**分けて**並べます。
どちらを信じるかが違うので、混ぜて見せません。

★AI には「迷っても選んでください」と伝えてあります（ご希望どおり）。
ただし**ジャンル名は手元にあるものからしか選ばせません。**
新しい名前を作られると、カラムが知らない名前で埋まってしまうためです。

### 0.25.0 で増えたこと（0.43.1 にも入っています）

**「自分の音源」―― 自分で録ったものを、くじと AI から外せるようになりました。**

本人の話:

> 自分のバンドで作曲途中のデータがたくさんあって
> それが読み込まれてるんです。これはシャッフルの対象にされたくない
> というのがありました。それで、タグなしを表示しないとか、
> そういうことを考えていたという経緯がありました。

★**「タグ無しを隠す」は、この用途に半分しか効いていませんでした。**

```
自分のバンド 4 組   453 曲

  タグ無し   206 曲  ← これだけが消えていた
  タグあり   247 曲  ← ★そのまま、くじにも AI にも入っていた
```

iTunes の頃にタグが付いてしまったものが半分あり、そこが素通りしていました。

#### 使い方

一覧でその演者の曲を 1 曲でも選ぶと、**「◯◯ を自分の音源にする（N 曲）」**が出ます。
1 曲選べば、その演者ぜんぶが入ります。

| | |
|---|---|
| 一覧 | **出ます。押せば鳴ります**（`✎` が付きます） |
| シャッフル | 入りません |
| AI DJ・Resonance の候補 | 入りません |
| 曲そのもの | **消えません。いつでも戻せます** |

「聴きたくない」ではなく「不意に流れてほしくない」ためのものなので、
一覧からは消しません。探せば聴けます。

#### ★覚えるのは演者名です

曲のパスではありません。パスで覚えると、走査し直したときや
新しく録ったぶんが、また混ざってしまいます。
実測でも、演者名なら 453 曲すべて拾えました（フォルダだと 451 曲でした）。

★**完全一致**で見ます。部分一致にすると、こうなります。

```
短い一語の名前で当てると…

  その名前ちょうど        17 曲  ← 自分のバンド
  その名前を含む別のバンド 10 曲  ← ★別人の作品。巻き込んではいけない
```

#### 0.24.1 の「盤の多い演者＝王道」も、これで直ります

自分のバンドの 1 組は、録った回ごとに盤が分かれていて **20 枚**あり、
0.24.1 はそれを「長く作っている＝王道」と読んでいました。
自分の音源は候補に入らなくなるので、王道の判定にも効かなくなります。

### 0.24.1 で直したこと（0.43.1 にも入っています）

**文脈の強度の足場を、曲数から「作品数 → 曲数」に変えました。**

本人の指摘:

> 文脈の強度を曲数で調べるってしましたが、曲数の多いグラインドコアだと
> それが効かなくて曲数の多いマニアックなバンドが王道に入ってきました。
> そこで、アルバムの数や作品数の数を調べてから曲数を調べた方が
> いいのではないか？と思いました。

★測ったら、指摘の通りでした。**曲数は盤の作りに引きずられます。**

```
1 枚あたり何曲入っているか（ジャンルごと）

  14.6 曲/枚   Gore Metal
  11.9 曲/枚   Grindcore
   8.3 曲/枚   Hardcore
   5.4 曲/枚   Powerviolence
```

同じ「100 曲」でも、ジャンルによって意味が 3 倍近く違います。
さらにディスコグラフィ盤は、**1 枚に 110 曲**入っていることがあります。

```
ジャンル「Hardcore」… 曲数の上位 20 組と作品数の上位 20 組で
                      重なるのは 6 組だけ（30%）

  曲数だと上位に来ていた     Larm        75 曲 / 1 枚
                            Los Crudos  74 曲 / 1 枚   ★どちらも編集盤
  作品数だと上位に来る       20 枚の演者
                            10 枚の演者     ★長く作っている
```

グラインドコアでも同じでした。

```
  曲数だと上位          Yacøpsæ    289 曲 / 4 枚（1 枚あたり 72 曲）
                       xBrainiax  130 曲 / 2 枚（1 枚あたり 65 曲）

  作品数だと上位に入る   Unholy Grave  9 枚 / 70 曲
                       Phobia        6 枚 / 89 曲
                       Pigsty        6 枚 / 65 曲
```

**AI に渡す候補が変わりました。**

```
  これまで   Napalm Death(336)
  これから   Napalm Death(20枚/336曲)
```

| 強度 | どう効くか |
|---|---|
| 王道 | **枚数の多い**演者を軸に（長く作ってきた演者） |
| 外す | **1〜2 枚しか持っていない**演者を積極的に |

曲数は、同じ枚数どうしを比べるときの目安として残してあります。
外すほうでは「曲数は見ないでください」と断っています
（1 枚しか無くても曲数が多いことがあるためです）。

### 0.24.0 で増えたこと（0.43.1 にも入っています）

**散らかったジャンル名を、AI がまとめてくれるようになりました。**

本人の希望:

> ジャンル名が適当に付けられたデータがたくさんあって困ってるんですが、
> AI がいい感じにまとめてくれる機能って作れますか？

★先に測りました。荒れ方は**打ち間違いではなく、粒の大きさのばらつき**でした。

```
86,044 曲 ／ ジャンル名 98 種類

  同じ系統が、大きい名前と細かい名前に散っている（いちばん大きい）
  大文字小文字だけが違うもの      3 組（合わせて 11,596 曲）
  1 つの欄に複数を詰め込んだもの   6 件
  ジャンルではないもの（レーベル名など）が混在
  「ジャンル名無し」という文字列   2,585 曲
```

**🏷 ジャンル名をまとめる** を押すと、AI が親にまとめた案を出します。
**押す前に、何がどう変わるかを全部見せます。**
要らない組はチェックを外せます。外したものは元の名前のまま残ります。

まとめると **🏷 ジャンル（まとめ）** タブが増えます。
いつもの 3 段（絞る・複数選ぶ・打ち込んで探す）がそのまま使えて、
**AI DJ と Resonance の組む範囲にもそのまま効きます。**

#### ★元のジャンル名には触りません

まとめは**別の層**として覚えておき、見るときに重ねるだけです。

・mp3 のタグは書き換えません（86,044 個のファイルを書き換えるのは戻せません）
・元のジャンル名はそのまま残り、ジャンルのタブでこれまで通り使えます
・**まとめをやめる** を押せば、完全に元通りになります

#### 送るもの・かかるもの

```
送るもの   ジャンル名と曲数だけ（2,147 文字 ≒ 614 トークン）
           ★曲は 1 曲も送りません
1 回        約 34 円
```

まとめるのは「見て判断する」仕事なので、考える深さを中くらいにしてあります。
費用のほとんどはその考えるぶんです。何度も押すものではないので、そこは削っていません。

★AI が手元に無い名前を返してきても、**必ず濾して**、手元にある名前だけを残します。
落としたものは黙って捨てず、画面に出します。

### 0.23.1 で直したこと（0.43.1 にも入っています）

**再生リストを聴いている最中に組むと、そのリストの中だけから選んでいました。**

本人の報告:

> ライブラリを開いて聞いてる時に AI DJ や Resonance で新しいプレイリストを
> 作ろうとして、プレイリスト内の曲内で再度プレイリストを作る誤爆を
> 何度もやってしまいます。
> カラム上ではジャンル選択をするからちゃんと選んでる気になって操作してしまいます。

こちらの作りが原因でした。
**流すときの「いまの列」を、組むときにもそのまま使っていました。**
再生リストを開いていると、それはリストの中身になります。

```
蔵書 100 曲 ／ カラムで選んで 60 曲 ／ 開いている一本 3 曲

  直す前  … AI に渡る候補  3 曲 ★カラムの 60 曲が丸ごと無視される
  直した後 … AI に渡る候補 50 曲 　カラム通り
```

**組む元は、いつもライブラリ＋カラムの絞り込みにしました。**
カラムは目に見えていて、自分で触っているもの。そこが範囲であるべきです。
再生リストのタブは「いま聴いているもの」であって、「選ぶ元」ではありません。

★**流すほうは変えていません。** 開いた一本を流すのは、そのままが正しい動きです。

**そして、押す前にどこから組むのかが見えるようにしました。**

```
組む範囲: 2,843 曲（ジャンル: Metal Core）

組む範囲: 2,843 曲（ジャンル: Metal Core）
　※ いま「ゆうべの一本」を聴いていますが、組む元はライブラリです
```

カラムを触るたびに書き換わります。ジャンル名は、カラムに出ているのと同じ字面で出します。

### 0.23.0 で変わったこと（0.43.1 にも入っています）

**ジャンルを 1 つに絞ったとき、つまみが効かなかったのを直しました。**

測ったら、理由が 2 つありました。

```
ジャンル「Metal Core」… 2,843 曲 ／ 232 組

  幅1（候補  50）→ 演者  50 組（この範囲の 22%）
  幅3（候補 200）→ 演者 200 組（86%）
  幅4（候補 400）→ 演者 232 組（100%）★ここで全部
  幅5（候補 800）→ 演者 232 組（100%）★増えない
```

**① 対象の幅が頭打ちになっていました。**
幅 4 でその範囲の演者が全部入るので、5 にしても候補は増えません。
いまは「この範囲は N 曲で全部なので、対象の幅を広げても増えません」と出します。

**② 文脈の強度に、足場がありませんでした。**
ひとつのジャンルに絞ると、候補はどれも「あなたの Metal Core」です。
そこへ「王道を」「外して」とだけ言っても、AI に拠り所がありません。

いまは候補に**その演者を何曲持っているか**を添えます。

```
1  Straightout(10)           Emotion Than Suicide
2  Scars Of Tomorrow(22)     The Unknown
3  A Heart Fades Forever(4)  Midst The Eternity Of Tomorrow
```

| 強度 | どう効くか |
|---|---|
| 王道 | 数の**大きい**演者を軸に（あなたにとっての王道） |
| 外す | **1〜2 曲しか持っていない**演者を積極的に（あなたの端） |

★一般的な有名・無名ではなく、**あなたの蔵書の中での遠近**で効きます。
実測で 65 曲〜1 曲まで幅がありました。

### 0.22.1 で直したこと（0.43.1 にも入っています）

**書き出す m3u の名前を、音蔵で付けた再生リストの名前にしました。**
`playlist.m3u` のままだと、スマホで取り込んだときに**どれも「playlist」**になって
見分けがつきませんでした。中にも `#PLAYLIST:` で名前を書くので、
プレイヤーの一覧にも音蔵と同じ名前で並びます。

```
Otogura_腐液のゴボゴボ行進/
  腐液のゴボゴボ行進.m3u        ← 中に #PLAYLIST:腐液のゴボゴボ行進
  01 - Napalm Death - Scum.mp3
```

★#PLAYLIST には**飾りを落とさない名前**を書きます。ファイル名のほうは
スマホで使えない字を落とすので、絵文字や記号が変わることがあります。

**左の一覧を狭めても、名前が潰れないようにしました。**
幅で縮めると名前の頭だけが並んで、**どれがどれか分からなくなっていました。**
いまは名前を元の長さのまま置いて、**横にも縦にも送れます**。

### 0.22.0 で変わったこと（0.43.1 にも入っています）

**「📱 スマホへ持ち出す」を足しました。一本ぶんの曲だけを運びます。**

再生リストを開いて押すと、その一本に**紐づいた曲だけ**をフォルダにコピーし、
相対パスの m3u を一緒に置きます。スマホに繋いでフォルダごとコピーすれば、
Android のどのプレイヤーでも**音蔵の並び順どおり**に鳴ります。

```
Otogura_🤖 夜のグラインド/
  playlist.m3u                       ← 相対の名前だけ
  01 - Napalm Death - Scum.mp3
  02 - Nasum - ….mp3
```

★測って決めました。

```
1 曲の平均    4.6 MB
一本 30 曲   139 MB   ← 楽に入る
ライブラリ全部 388 GB   ← スマホには入らない
形式          .mp3 が 86,044 曲（全部）→ 変換は要りません
```

★**元のファイルは触りません。コピーするだけ**です（移動も削除もしません）。
★持ち出す先に残っている知らないファイルも**消しません**。数だけ知らせます
（置き場を間違えて指されたときに、消してしまうほうが怖いので）。
★曲名の頭に番号を付けます。m3u を読まないプレイヤーでも並び順どおりに鳴ります。
★スマホで使えない字（`/ : * ? " < > |`）は落とします。

★自動で同期したいときは、書き出し先を決まったフォルダにして、
そこを Syncthing のようなソフトに任せるのが確実です。

### 0.21.0 で変わったこと（0.43.1 にも入っています）

**再生リストが、左の縦一覧になりました。**
横一列だと、増えたぶんが右へ流れて見えなくなっていました。
8 本を超えると**探す欄**が出ます。左右の幅は掴んで変えられ、覚えます。

**自分でカラムを絞っているときは、AI にジャンルを選ばせません。**

```
ジャンルが「すべて」  → AI が自分の知識でジャンルを選ぶ（今までどおり）
ジャンルを選んでいる  → いま画面に出ている範囲から、そのまま組む
```

★自分でつけたジャンル名は、AI の知らない言葉のことがあります。
そこへ「一覧から選べ」と頼むと、**意味の分からない名前から選ばせる**ことになります。
絞ってあるなら、そのまま使うのが正しい。ジャンルだけでなく、演者・盤・日付・響きでも同じです。

**「🔀 この響きで一本を組む」を足しました。**
響きで選ばれた曲は多すぎてシャッフルしきれないので、**そこから AI が厳選して並べます。**

★響きの欄に組むボタンを置かない、と前に決めていました（0.13.0）。
あのときは押すつもりのないボタンが 2 つあって事故が起きたためです。
いまは **1 つだけ、何が起きるかを名前に書いて**置いてあります。押したときだけ動きます。

★左に一覧を置いたぶん、表の幅が狭くなったので列幅を組み直しました。
「シャッフルに入れない」は「くじに入れない」に短くしています（くじ＝このアプリの言葉）。

### 0.20.0 で変わったこと（0.43.1 にも入っています）

**つまみが 3 本目「文脈の強度」になりました。王道を守るか、外すか。**

```
対象の幅 [──●──] ふつう   選出の量 [──●──] 30 曲   文脈の強度 [──●──] ふつう
```

| 目盛 | 何が変わるか | 考える深さ |
|---|---|---|
| 1 王道 | ど真ん中を選ぶ。代表格で構わない | 浅い（安い） |
| 2 やや王道 | ど真ん中を軸に、少しだけ外す | 浅い |
| **3 ふつう** | **王道と外したものを半々で** | ふつう |
| 4 やや外す | 代表格は控えめに | ふつう |
| 5 外す | 代表格は出さない。迷ったら遠いほうへ | 深い（高い） |

★**ここは前まで決め打ちでした。** 0.17.0 で「王道に寄りすぎる」と言われたとき、
頼み文に「代表格は出さないで」と書き込んで固定しました。
つまり **いつも「外す」側**で、守ってほしいときに守れませんでした。

★**いままでと同じ効きにしたいときは、5 にしてください。**
既定は 3（ふつう）です。

★幅・量と同じで、**1 つのつまみが 🎧 と 🌱 の両方に効きます。**

★**外すほど深く考えさせるので、そのぶん高くなります。**
代表格を挙げるのは思い出すだけですが、遠いものを引き出すのは
深く考えないと「まず思いつくもの」が出てくるためです。

### 0.19.0 で変わったこと（0.43.1 にも入っています）

**「交差」を足しました ―― いくつもの言葉から辿り着いた名前です。**

別々の言葉が同じ名前を指すのは偶然ではありません。
その人の中で、いくつもの筋がそこへ通じている、ということです。

```
🌐 3 組・120 曲  [言葉…] [▶ 交差 4] [▶ 確かめる候補 12] [× 全部外す]

交差 ― いくつもの言葉から辿り着いた名前です。
  3 本  Napalm Death   354 曲  ［グラインドコア・carcass・ハードコア］
  2 本  Nasum          201 曲  ［グラインドコア・carcass］
```

★**言葉を辿るほど増えます。1 本目では絶対に起きません。**
これが「たくさん使うと効いてくる」ところです。

交差した名前は**選ばれやすくもなります**（本数ぶん強く。3 倍で頭打ち）。
AI に一本を組ませるときにも、交差していることを伝えます。
確かめる候補のほうにも出るので、**手元に無いなかで一番あやしいもの**が分かります。

★この情報は前から取れていたのに、**捨てていました。**
同じ名前が複数の木に出たとき「重いほうを採る」だけで、
何本の言葉から辿り着いたかを数えていませんでした。

### 0.18.2 で直したこと（0.43.1 にも入っています）

**「対象の幅」を最大にすると出ていたエラーを、本当に直しました。**

0.18.1 で返事の上限を広げたら、今度は SDK に断られていました。

```
Streaming is required for operations that may take longer than 10 minutes.
```

SDK の中に境目が書いてあります。

```
見込み時間 = 60分 × max_tokens / 128000
  10 分を超えるなら、流し込み（streaming）でないと断る
  → max_tokens 21,333 が、流し込みでないときの上限

幅3 … 17,250（通る）   幅5 … 25,500（★通らない）
```

**上限を下げて逃げるのではなく、受け取り方を流し込みに変えました。**
長い返事が要ることは変わらないので、上限を下げると別のところが切れます。
流し込みなら、この縛りがそもそもありません。

### 0.18.1 で直したこと（0.43.1 にも入っています）

**「対象の幅」を最大にすると、辿るのが失敗していたのを直しました。**

返事の上限が足りていませんでした。**考えたぶんも上限に含まれます。**
0.17.0 で考える深さを上げたとき上限を上げ忘れ、0.18.0 で名前を 60 → 90 個に
増やしたので、いちばん広いところで足りなくなりました。

```
幅5（名前 90 個）  返事に要る ≈ 5,400 ﾄｰｸﾝ / 前の上限 7,800
  → 考えるぶんが 2,400 しか残らない → 途中で切れる
いまの上限 25,500（上限は使った分だけの請求なので、大きめに取っています）
```

**失敗した理由が、下の欄に最後まで出るようになりました。**
上の欄は狭くて「…」で切れるので、何が起きたか読めませんでした。

### 0.18.0 で変わったこと（0.43.1 にも入っています）

**響きで出るバンドの数を増やしました。**

0.17.0 で「代表格を避けて」と入れたとき、**当たりが減る作りも一緒に入れてしまいました。**
頼み文に「半分くらいは一覧に無い名前を混ぜて」と書いてあり、
**当たりの上限を、こちらから半分に絞っていました。**

直したのは 3 つです。

```
1. 「半分は手元に無い名前を」をやめた
     → 手元にある名前を選んでよい（むしろ歓迎）と書き換え
2. AI に見せる演者を 150 組 → 800 組（354 曲〜1 曲の組まで、全体から等間隔）
     → 5,853 組あるのに 150 組しか見せていなければ、当てずっぽうになる
3. 辿る名前を 21 個 → 35 個（ふつうのとき）
```

★**発見は捨てていません。** 手元に無い名前は「確かめる候補」に出ます。
**鳴らせるバンドを増やすのが先で、発見はそちらで受けます。**

★**マニアックさも捨てていません。** この人の一覧そのものがマニアックなので、
「一覧から選ぶ」と「代表格を避ける」は両立します。

★**もっと欲しいときは、同じ言葉でもう一度「辿る」を押してください。**
押すたびに足されます。「対象の幅」を広げると、一度に出る数も増えます。

### 0.17.2 で直したこと（0.43.1 にも入っています）

**確認のあと、文字が打てなくなっていたのを直しました。**

遅いのではありませんでした（描き直しは 148 ms）。
**OS の確認ダイアログを閉じると、窓が焦点を失ったままになる**のが原因でした。

```
ダイアログの前  document.hasFocus() = true
閉じたあと      document.hasFocus() = false   ← ここ
```

記入欄の中では焦点が当たったままなので、**見た目は打てそうに見えます。**
でもキーは、OS が焦点だと思っている別の窓へ行きます。

こちらから焦点を戻す道も試しましたが、**当てになりませんでした**
（Windows は「裏の窓が勝手に前へ出る」のを止めるため）。
**だから OS に出すのをやめて、画面の中で訊くようにしました。**
`prompt()` が使えなかったときと、まったく同じ答えです。

### 0.17.1 で直したこと（0.43.1 にも入っています）

**★走査している間、文字が打てなくなっていたのを直しました。**
3 回報告をもらって、2 回再現できずに終わっていたものです。
速さではなく「詰まり」を測ったら出ました。

```
検査用の 3,000 曲で測った、本体が詰まった長さ
  何もしていないとき   最悪  14 ms
  走査している間       最悪 676 ms   ← ここ
  息継ぎを入れたあと   最悪   0 ms
```

Electron の本体は、画面にキー入力を届ける役でもあります。**そこが走査で
埋まると、押しても届きません。** 走査が進むと空くので、待てば打てるようになります
―― 「時間が経つと打てるようになった」の正体でした。
1 曲読むごと・フォルダ 1 つ数えるごとに息継ぎを入れました。**走査は遅くなりません。**

**同じ言葉でもう一度「辿る」と、名前が足されるようになりました。**
前は入れ替えていたので、AI が似た答えを返すと何も変わりませんでした。
いまは「すでに挙げた名前」を渡して、別のものを頼みます。

★**曲がたくさん当たるのに演者が少ないときは、同じ言葉で押し直してください。**
押すたびに演者が増えます（グラインドコアのように手元が厚いところで起きます）。

### 0.17.0 で変わったこと（0.43.1 にも入っています）

**響きが王道に寄りすぎるのを直しました。**

```
なぜ AI DJ のほうが深かったか ―― 見ているものが違いました
  AI DJ … 候補 200 曲を「実物で」渡す（手元にある曲名がそのまま並ぶ）
  響き  … 演者の名前だけ渡して、あとは AI の記憶から出させていた
記憶から出させると、まず挙がるのは代表格 ―― つまり王道になります。
```

渡し方にも穴がありました。**上位 150 組しか見せていなかった**ので、
細いところを持っていることが伝わっていませんでした。

```
演者 5,860 組。上位 150 組が占めるのは 15,226 曲（18%）だけ
150 番目でも 61 曲 ―― 「たくさん持つ人」にしか見えない
1 曲だけ持っている演者   932 組（15.9%）
4〜10 曲だけの演者    1,895 組（32.3%）
```

3 分の 1 を「1〜数曲だけ持っている演者」から渡すようにし、蔵書の大きさも伝えます。
そのうえで「まず名前が挙がる代表格は出さない」「深さ 3 はその筋の人しか挙げないもの」
と頼み文に書き、**辿るところだけ考える深さを上げました**（1 本 4 円 → 8 円ほど）。

**辿った言葉の札を 1 つにまとめました。**
前は［言葉］［✎］［×］が別々の丸で、言葉 3 つで丸が 9 個並んでいました。

**下の欄で、フォルダと状態の文が重ならないようにしました。**
状態はいつも自分の行に置きます（短く切ると大事な文が消えるので）。

**辿ったあと、記入欄の文字が残っていたのを直しました。**

### 0.16.0 で変わったこと（0.43.1 にも入っています）

**辿った名前のうち、手元で見つからなかったものを出せるようになりました。**
「🌐 響き」タブの「▶ 確かめる候補」を押すと開きます。

```
確かめる候補 ― 手元で見つからなかった名前です。演者名だけでなく、盤名・曲名にも
当ててみて、それでも見つからなかったものだけ出しています。
それでも「持っているのに書かれ方が違う」「AI の思い違い」は残ります。買う前に確かめてください。
  深さ3  ある名前   なぜ繋がるか　［「辿った言葉」から］
  深さ1  べつの名前 なぜ繋がるか　［「辿った言葉」から］
```

★**「買い物リスト」ではありません。** 見出しにもそう書いてあります。
ここから買う・落とす仕掛けは置いていません。出すのは一覧までです。

### 0.15.3 で直したこと（0.43.1 にも入っています）

**音楽フォルダを 1 つも登録していないと、曲があるのに 0 曲の画面になっていました。**
覚えている一覧を出さず、理由も言わないので、**消えたようにしか見えません。**
いまは前に読んだぶんをそのまま出して、フォルダを足すよう案内します。

**さらに、その状態で走査すると覚え書きが消えるところでした。**
走査は「見つかったものが、そのまま新しい全部」という作りなので、
探す場所が無いと 0 件見つかり、**86,044 件の記録を空で上書き**します。
フォルダが無いときは、走査そのものをしないようにしました。

```
実測（覚え書き 86,044 件）
  フォルダ 0 個で走査 → 曲 0 / 覚え書き 0 件（50 分ぶんが消える）
```

★「止めたら消えた」（0.14.1）と同じ形の間違いです。あちらは途中で止めたとき、
こちらは**そもそも探す場所が無いとき**。どちらも「見つからない」を
「無くなった」と取り違えていました。

### 0.15.2 で変わったこと（0.43.1 にも入っています）

**AI の一本で、同じバンドが重ならないようになりました。**
頼み文に「続けない」と書くだけでは効いていませんでした ―― **候補そのものが
偏っていた**からです。曲を 1 曲ずつ引くと、持っている曲数が多いバンドほど
何度も当たります。演者ごとに 1 曲ずつ取る引き方に変えました。

```
候補 200 曲の中で、同じ演者が最多で何曲か（実測・85,327 曲）
  全部 85,327 曲        2.2 曲 → 1.0 曲
  Hardcore 12,537 曲    4.4 曲 → 1.0 曲
  演者 30 人 5,263 曲  17.8 曲 → 7.0 曲（30 人で 200 曲なら 7 が下限）
  演者 5 人 1,667 曲   53.4 曲 → 40.0 曲（5 人で 200 曲なら 40 が下限）
```

**押して組めたら、記入欄の文字が消えるようになりました。**
（だめだったときは消しません。書き直すのに、もう一度打たせないため）

**下端のボタンの名前を「🔑 APIキーを入れる」に変えました。**
もとは「🤖 気分で選ぶ」でしたが、押してもキーを入れるだけで選曲はしません。
入っているときは「🔑 APIキーを消す」に変わります。

### 0.15.1 で直したこと（0.43.1 にも入っています）

**🌱 辿ったときに、AI の一本まで一緒にできてしまうのをやめました。**
辿った結果は「🌐 響き」タブそのものです。そこを 🔀 で流してください。
通信も 1 往復だけになります（辿るところだけ）。

**一覧の「一覧から外す」が見えていませんでした。**
操作の欄が 92px しかないのに、文字は 187px 要りました。
**「一覧から外す」は 1 文字も出ていませんでした。** 欄を広げました。

### 0.15.0 で変わったこと（0.43.1 にも入っています）

**AI に頼む「対象の幅」と「選出の量」を、つまみで決められるようになりました。**
つまみは **1 組だけ**で、🎧 気分からも 🌱 言葉から辿るほうも、**両方に効きます**。

```
対象の幅 [──●──] ふつう   選出の量 [──●──] 30 曲
候補 200 曲 ／ 辿る名前 21 個 ／ ジャンル 5 まで　→ 30 曲の一本（目安 7.4 円）
```

**いま何が起きるか、動かす前に出ます。** 何曲が候補になり、いくらかかるか。

★**触らなければ、今までとまったく同じ動きです。** 真ん中が今までの値です。

### 0.14.1 で直したこと（0.43.1 にも入っています）

**止めると一覧が消えていたのを直しました（0.14.0 の不具合）。**
止めたときは、まだ見ていないぶんを前の記録から補います。
**覚え書きも減りません。**

### 0.14.0 で変わったこと（0.43.1 にも入っています）

**読み込みを途中で止められるようになりました。**
走査中は「再スキャン」が「**■ 止める**」に変わります。

**止めても損はしません。** そこまで読んだぶんは残り、次は続きから進みます。

### 0.13.0 で変わったこと（0.14.0 にも入っています）

**記入欄を 2 つに分けました。**

```
🎧 [いまの気分を書く]  [一本を組む]     🌱 [言葉を 1 つ入れて辿る]  [辿る]
```

**一本ができても、勝手に流れなくなりました。** ▶ を押してから流れます。

**辿った言葉を直す欄は、「🌐 響き」タブを開いているときだけ**出ます。

### 0.12.0 で変わったこと（0.13.0 にも入っています）

**辿った言葉の名前を変えたり、消したりできるようになりました。**

### 0.11.0 で変わったこと（0.12.0 にも入っています）

**辿った結果が、3 カラムの「🌐 響き」タブに入るようになりました。**
辿った言葉 → 演者 → アルバム の 3 段で絞れます。

**その結果を、そのまま／混ぜて、再生リストに残せます。**

### 0.10.0 で変わったこと（0.11.0 にも入っています）

**言葉を 1 つ入れると、そこから辿れる音楽を探して、手元にあるものを教えてくれます。**

```
🌱 [言葉を 1 つ入れて辿る]  →  21 個辿りました ― 手元に 9 個
   手元にあったもの … 3 カラムの「🌐 響き」タブに入ります
   手元に無いもの   … こちらが「発見」です
```

見つかった演者は選曲にも効きます（AI が一本を組むとき、その辺りを優先します）。

### 0.9.0 で変わったこと（0.10.0 にも入っています）

**決まった形の JSON（辿った木）を読み込めるようになりました。**
「響いた言葉の周りにあった名前」が手元にあれば、選曲に効きます。
（0.10.0 からは、読み込まなくても音蔵の中で辿れます）

### 0.8.0 で変わったこと（0.9.0 にも入っています）

**気分を書くと、AI が「一本」を組んで、再生リストとして残すようになりました。**
絞り込むだけだったのが、**流す順に 30 曲並べて、そのまま流し始めます。**

### 0.7.0 で変わったこと（0.8.0 にも入っています）

**気分を書くと、聴く範囲を選んでくれる機能を足しました（任意）。**
Anthropic の API キーを入れたときだけ出ます。**入れなければ、これまでどおりです。**

### 0.6.0 で変わったこと（0.7.0 にも入っています）

**シャッフルで、同じアーティスト・同じアルバムが続かなくなりました。**

### 0.5.0 で変わったこと（0.6.0 にも入っています）

**3 カラムに「日付」タブが増えました。** 年 → 月 → 日 で絞り込めます。

**タブを切り替えても、前のタブの絞り込みは残ります。**
あるジャンルを選んでから日付タブで年を選べば、その掛け合わせになります。

### 0.4.0 で変わったこと（0.5.0 にも入っています）

**「日付」の列が増えました。押すと新しい順に並びます。**

手に入れた時期で探せます。もう一度押すと古い順。

### 0.3.0 で変わったこと（0.4.0 にも入っています）

**3 カラムで複数選べるようになりました。**
Ctrl+クリックで 1 つずつ足す／外す、Shift+クリックで範囲。
シャッフルの対象は下の一覧なので、**選んだぶんがそのままシャッフルの範囲**になります。

**曲ごとにシャッフルから外せるようになりました。**
一覧の「シャッフルに入れない」。**一覧には残り、押せば鳴ります。**
くじで選ばれなくなるだけです（「一覧から外す」とは別物）。

くわしくは下の「絞り込みは複数選べます」と「シャッフルに入れたくない曲」に書いています。

### 0.2.0 で変わったこと（0.3.0 にも入っています）

**落とし直す値打ちがあるのは、この 3 つです。**

**1. mp3 以外も読むようになりました（`.m4a` `.wav` `.flac`）**

CD からの取り込み機は、MP3 のつもりでも `.m4a` で書き出すものがあります。
0.1.0 は `.mp3` しか見ていなかったので、**一覧に出ないのではなく、
そもそも見つけていませんでした**。
作者の手元では、それで **1 万曲あまり**が抜けていました
（10,121 曲。うち鳴らせるのは 9,684 曲）。

足す形式は**実際に鳴らして確かめたものだけ**にしてあります。
aiff と m4p は鳴らないので拾いません。
`.m4a` のうち **ALAC（Apple ロスレス）は鳴りません** ―― こちらは一覧には出したうえで、
押したときに理由を出します（持っている曲が黙って消えるほうが困るので）。

**2. シャッフルの対象が「下の一覧に出ているぶん」になりました**

0.1.0 は**押した時点の一覧を覚える**作りでした。
そのため、アルバムを選んで再生したあと全体シャッフルに戻すには、
**一度止めて選び直す**必要がありました。
いまは 3 カラムで選び直せば、**止めずにそのまま**対象が変わります。

**3. ▶ を押すだけで流し始められます**

0.1.0 は、先に曲を 1 つ押さないと ▶ が効きませんでした。
いまはシャッフルを入れて ▶ を押すだけで流れ始めます。

★ほかに、100 MB を超える曲は音量そろえを測らなくなりました
（測定はファイル全体をメモリに載せるので、大きい wav では持ちません）。
測らないだけで**再生はできます**。
