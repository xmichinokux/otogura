// シャッフルが本当に「再生回数の少ない曲」を選びやすいか、実際に引いて数える
const { 次を選ぶ, 巡が終わったか } = require('./src/shuffle');

const 曲 = {
  'よく聴く曲（50回）': 50,
  'たまに聴く曲（10回）': 10,
  '数回聴いた曲（2回）': 2,
  '一度だけ聴いた（1回）': 1,
  '忘れている曲A（0回）': 0,
  '忘れている曲B（0回）': 0,
};
const 候補 = Object.keys(曲);
const 回数表 = 曲;

/* 1. 1万回引いて、どれが何回出たか（巡は使わず、重みだけ見る） */
const 出た = Object.fromEntries(候補.map((p) => [p, 0]));
const N = 10000;
for (let i = 0; i < N; i += 1) {
  const p = 次を選ぶ(候補, 回数表, new Set());
  出た[p] += 1;
}

console.log(`■ ${N} 回引いた結果（均等なら各 ${(100 / 候補.length).toFixed(1)}%）\n`);
for (const p of 候補) {
  const 率 = (出た[p] / N) * 100;
  const 棒 = '█'.repeat(Math.round(率));
  console.log(`  ${p.padEnd(22)} ${率.toFixed(1).padStart(5)}%  ${棒}`);
}

const 忘れ = (出た['忘れている曲A（0回）'] + 出た['忘れている曲B（0回）']) / N;
const よく = 出た['よく聴く曲（50回）'] / N;
console.log(`\n  忘れている曲（0回）の合計 : ${(忘れ * 100).toFixed(1)}%`);
console.log(`  よく聴く曲（50回）        : ${(よく * 100).toFixed(1)}%`);
console.log(`  → 0回の曲は 50回の曲の約 ${Math.round((忘れ / 2) / よく)} 倍 出やすい`);

/* 2. 同じ曲が続けて出ないか（巡を使う） */
console.log('\n■ 巡を使うと、同じ曲が続けて出ないか');
let 済み = new Set();
const 並び = [];
for (let i = 0; i < 候補.length; i += 1) {
  const p = 次を選ぶ(候補, 回数表, 済み);
  並び.push(p);
  済み.add(p);
}
const 重複 = 並び.length - new Set(並び).size;
console.log(`  1巡で出た曲: ${並び.length} 曲 / 重複 ${重複} 件 ${重複 === 0 ? '✓' : '★'}`);
console.log(`  巡が終わったと判定できるか: ${巡が終わったか(候補, 済み) ? '✓' : '★'}`);

/* 3. 締め出していないか（よく聴く曲も、たまには出る） */
console.log('\n■ よく聴く曲も締め出していないか');
console.log(`  50回の曲が 1万回中に出た回数: ${出た['よく聴く曲（50回）']} 回 ${出た['よく聴く曲（50回）'] > 0 ? '✓ 出る' : '★ 出ない'}`);

/* ───────────────────────────────────────────────
   4. 画面側の「対象の決め方」（2026-08-29 本人の希望 2 件）

     > シャッフルを選んだ時、再生ボタンを押すと最初の一曲目から
     > ランダム再生してほしい。今は自分で一曲目を選ばないといけない
     > シャッフルの対象は下のカラムに表示されてる曲のリストにできないかな？

   ★どちらも「開いて押すまで分からない」種類なので、機械に押させる。
   画面のコード（renderer.js）は window / document があって Node から
   そのままは呼べないので、**その部分だけ切り出して**動かす（check-sort.js と同じ）。
   切り出しに失敗したら黙って通さず、はっきり落とす。
   ─────────────────────────────────────────────── */
console.log('\n■ 対象の決め方（いまの列 / 流し始める / 次の曲 / 送る）');

const fs = require('node:fs');
const path = require('node:path');

let 失敗4 = 0;
const 確認4 = (名, 条件, 補足 = '') => {
  if (条件) { console.log(`  OK   ${名}`); } else { console.log(`  NG   ${名}${補足 ? ' ― ' + 補足 : ''}`); 失敗4 += 1; }
};

const 画面 = fs.readFileSync(path.join(__dirname, 'src/renderer.js'), 'utf8');
const 始め4 = 画面.indexOf('function いまの列()');
const 終わり4 = 画面.indexOf('\n}', 画面.indexOf('function 送る(方向)'));
if (始め4 < 0 || 終わり4 < 0) {
  console.log('  NG   renderer.js から「対象の決め方」を切り出せませんでした');
  process.exit(1);
}
const 切り出し4 = 画面.slice(始め4, 終わり4 + 2);

/* 画面の変数・関数の代わり（ここに無いものを使い始めたら、下の eval が落ちる） */
function 台を作る() {
  const 曲を作る = (i, 属性 = {}) => ({
    path: `C:/m/${属性.artist ?? 'A'}/${i}.mp3`,
    title: `曲${i}`, artist: 属性.artist ?? 'A', album: 属性.album ?? 'AL', genre: 'G',
    track: i, タグあり: true, ...属性,
  });
  return {
    tracks: [
      曲を作る(1, { artist: 'あ' }), 曲を作る(2, { artist: 'あ' }),
      曲を作る(3, { artist: 'い' }), 曲を作る(4, { artist: 'い' }),
      曲を作る(5, { artist: 'う' }),
    ],
    曲を作る,
  };
}

function 走らせる(仕込み) {
  const 環境 = {
    tracks: 仕込み.tracks,
    sel: 仕込み.sel ?? { genre: null, artist: null, album: null },
    絞った回数: 0,
    流したもの: [],
    出た文字: [],
    nowPath: 仕込み.nowPath ?? null,
  };
  /*
   * ★画面の変数は「素の let」として用意する。
   * 切り出した本文を書き換えて動かそうとすると、
   * 「巡」が「巡が終わったか」の中まで置き換わるような事故が起きる（実際に起きた）。
   * 書き換えないのがいちばん安全。
   */
  const 前置き = `
    /* ★画面の文は 言() を通る。ここは訳さず、そのまま返せばいい */
    const 言 = (文) => String(文);
    const 開いているリスト = () => null;
    const 曲を並べる = (a, b) => (a.track ?? 0) - (b.track ?? 0);
    function 絞る(level) {
      環境.絞った回数 += 1;
      return 環境.tracks.filter((t) => {
        if (level > 1 && 環境.sel.artist && t.artist !== 環境.sel.artist) return false;
        return true;
      });
    }
    const $ = () => ({ set textContent(v) { 環境.出た文字.push(String(v)); }, get textContent() { return ''; } });
    const audio = { pause() {}, getAttribute: () => null };
    function 再生する(t, o) {
      環境.流したもの.push(t.path);
      nowPath = t.path; 環境.nowPath = t.path;
      if (!o || !o.列を保つ) 巡 = new Set();
      巡.add(t.path);
    }
    let tracks = 環境.tracks;
    let nowPath = 環境.nowPath;
    let 巡 = new Set(仕込み.巡 ?? []);
    let シャッフル = !!仕込み.シャッフル;
    let 繰り返し = 仕込み.繰り返し ?? 'none';
    let 再生回数 = 仕込み.再生回数 ?? {};
    let シャッフル除外 = new Set(仕込み.シャッフル除外 ?? []);
    /*
     * ★自分の音源（2026-08-30）。くじの引き方が これも見るようになった。
     * 仕込みで演者名を渡せば、ここでも試せる。
     */
    const 自分の音源 = new Set(仕込み.自分の音源 ?? []);
    const 自分のか = (t) => 自分の音源.has(String((t && t.artist) || '').toLocaleLowerCase('ja').trim());
    const つなぎか = () => false;   // ★つなぎの小品（2026-08-30）。ここでは誰もつなぎではない
    void つなぎか;
    void 自分のか;
  `;
  // eslint-disable-next-line no-new-func
  const 動かす = new Function('環境', '仕込み', '次を選ぶ', '巡が終わったか',
    `${前置き}
${切り出し4}
 return { いまの列, 流し始める, 次の曲, 送る };`);
  return { 環境, ...動かす(環境, 仕込み, 次を選ぶ, 巡が終わったか) };
}

const 台 = 台を作る();

/* 4-1. ▶ を押すだけで流れ始めるか（シャッフル ON） */
{
  const { 環境, 流し始める } = 走らせる({ tracks: 台.tracks, シャッフル: true });
  流し始める();
  確認4(
    '★シャッフル ON で、曲を選ばずに ▶ だけで流れ始める',
    環境.流したもの.length === 1,
    `流れたもの: ${JSON.stringify(環境.流したもの)}`,
  );
}

/* 4-2. シャッフル OFF なら並びの頭から */
{
  const { 環境, 流し始める } = 走らせる({ tracks: 台.tracks, シャッフル: false });
  流し始める();
  確認4('シャッフル OFF なら並びの先頭から', 環境.流したもの[0] === 台.tracks[0].path, `${環境.流したもの[0]}`);
}

/* 4-3. 鳴らせない曲を頭に置いても、それは選ばない */
{
  const 鳴らない = 台.曲を作る(0, { artist: 'あ', 鳴らせる: false });
  鳴らない.track = 0;                                  // 並びの先頭に来る
  const { 環境, 流し始める } = 走らせる({ tracks: [鳴らない, ...台.tracks], シャッフル: false });
  流し始める();
  確認4('★鳴らせない曲は、押していきなり選ばれない', 環境.流したもの[0] !== 鳴らない.path, `${環境.流したもの[0]}`);
}

/* 4-4. ここが本題 ―― 絞り込みを変えたら、対象もそこに変わるか */
{
  const { 環境, いまの列 } = 走らせる({ tracks: 台.tracks });
  const 全部 = いまの列().length;
  環境.sel.artist = 'あ';                              // 再生を止めずに絞り込みだけ変える
  const 絞った後 = いまの列().length;
  確認4(
    '★絞り込みを変えると、対象もそこに変わる（止めなくていい）',
    全部 === 5 && 絞った後 === 2,
    `全部 ${全部} 曲 → 「あ」で絞ると ${絞った後} 曲`,
  );

  環境.sel.artist = null;                              // 選び直せば、また全体に戻る
  確認4('絞り込みを外すと、また全体が対象になる', いまの列().length === 5);
}

/* 4-5. 送る で対象を作り直しすぎていないか（86,000 曲を毎回並べ直さない） */
{
  const 鳴らない = (i) => 台.曲を作る(i, { artist: 'あ', 鳴らせる: false });
  const 並び = [台.曲を作る(1, { artist: 'あ' }), 鳴らない(2), 鳴らない(3), 鳴らない(4), 台.曲を作る(5, { artist: 'あ' })];
  const { 環境, 送る } = 走らせる({ tracks: 並び, nowPath: 並び[0].path });
  送る(1);
  確認4(
    '鳴らせない曲を飛ばして、鳴る曲まで進む',
    環境.流したもの[0] === 並び[4].path,
    `流れたもの: ${JSON.stringify(環境.流したもの)}`,
  );
  確認4(
    '★飛ばした数だけ絞り直していない',
    環境.絞った回数 === 1,
    `絞った回数: ${環境.絞った回数}（飛ばすたびに絞ると 86,000 曲では持ちません）`,
  );
  確認4('飛ばしたことを黙っていない', 環境.出た文字.some((s) => s.includes('飛ばしました')), JSON.stringify(環境.出た文字));
}

/* 4-6. シャッフルに入れない曲は、くじの候補から外れるか（2026-08-29 本人の希望） */
{
  const 並び = [台.曲を作る(1), 台.曲を作る(2), 台.曲を作る(3)];
  const 外す = [並び[0].path, 並び[1].path];
  // 3 曲のうち 2 曲を外したら、くじは必ず残りの 1 曲を選ぶ。30 回引いて確かめる
  let 外れた = null;
  for (let i = 0; i < 30; i += 1) {
    const { 環境, 流し始める } = 走らせる({ tracks: 並び, シャッフル: true, シャッフル除外: 外す });
    流し始める();
    if (環境.流したもの[0] !== 並び[2].path) { 外れた = i + ' 回目に ' + 環境.流したもの[0]; break; }
  }
  確認4('★シャッフルから外した曲は選ばれない', 外れた === null, 外れた || '');
}

/* 4-7. 全部外したら、黙って止まらずに理由を出す */
{
  const 並び = [台.曲を作る(1), 台.曲を作る(2)];
  const { 環境, 流し始める } = 走らせる({
    tracks: 並び, シャッフル: true, シャッフル除外: 並び.map((t) => t.path),
  });
  流し始める();
  確認4('全部外したら流さない', 環境.流したもの.length === 0);
  確認4('★なぜ流れないかを画面に出す', 環境.出た文字.some((t) => t.includes('シャッフルから外して')), JSON.stringify(環境.出た文字));
}

/* 4-8. 除外はシャッフルのときだけ。並び順で送るときは流す */
{
  const 並び = [台.曲を作る(1), 台.曲を作る(2), 台.曲を作る(3)];
  const { 環境, 送る } = 走らせる({
    tracks: 並び, シャッフル: false, nowPath: 並び[0].path, シャッフル除外: [並び[1].path],
  });
  送る(1);
  確認4(
    '★並び順で送るときは、外した曲も流す（除外はシャッフルだけ）',
    環境.流したもの[0] === 並び[1].path,
    '流れたもの: ' + JSON.stringify(環境.流したもの) + '（飛ばすと「一覧から外す」と同じになってしまいます）',
  );
}

/* ───────────────────────────────────────────────
   5. 3 カラムの複数選択（2026-08-29 本人の希望）

     > 3カラム部分をctrlやshiftを押しながらクリックすることで
     > 複数選べるようにできないかな？
     > その上で、個別に指定を外すことはできないかな？

   ★押し方（ふつう／Ctrl／Shift）で結果が変わるところなので、実際に押させる。
   ─────────────────────────────────────────────── */
console.log('\n■ 3 カラムの複数選択（絞る / 選ぶ）');

/* ★署名が変わっても切り出せるように、名前だけで探す（2026-08-31）*/
const 始め5 = 画面.indexOf('function タブに合う(');
const 終わり5 = 画面.indexOf('\n}', 画面.indexOf('function 選ぶ(level, 値, e = null)'));
if (始め5 < 0 || 終わり5 < 0) {
  console.log('  NG   renderer.js から「絞る / 選ぶ」を切り出せませんでした');
  process.exit(1);
}
const 切り出し5 = 画面.slice(始め5, 終わり5 + 2);

function 列を走らせる(曲たち) {
  const 環境 = { tracks: 曲たち, 描き直した: 0 };
  const 前置き = `
    /* ★画面の文は 言() を通る。ここは訳さず、そのまま返せばいい */
    const 言 = (文) => String(文);
    const 見える曲 = () => 環境.tracks;
    const 小文字 = (v) => String(v).toLocaleLowerCase('ja');
    let sel = { genre: null, artist: null, album: null, 年: null, 月: null, 日: null };
    let カラムタブ = 環境.カラムタブ ?? 'tag';
    // ★テンプレートリテラルは使わない。ここ自体が前置きのテンプレートの中なので、
    //   ドル記号と波括弧の並びが、外側に食われるため（実際に一度やった）。
    const 日付 = (ms) => { const d = new Date(ms); const 二 = (n) => String(n).padStart(2, '0');
      return d.getFullYear() + '-' + 二(d.getMonth() + 1) + '-' + 二(d.getDate()); };
    const 年月日 = {
      年: (t) => (t.更新日時 > 0 ? String(new Date(t.更新日時).getFullYear()) : null),
      月: (t) => (t.更新日時 > 0 ? 日付(t.更新日時).slice(0, 7) : null),
      日: (t) => (t.更新日時 > 0 ? 日付(t.更新日時) : null),
    };
    const カラムタブの列 = {
      tag: [
        { key: 'genre', 取る: (t) => t.genre },
        { key: 'artist', 取る: (t) => t.artist },
        { key: 'album', 取る: (t) => t.album },
      ],
      date: [
        { key: '年', 取る: 年月日.年, 新しい順: true },
        { key: '月', 取る: 年月日.月, 新しい順: true },
        { key: '日', 取る: 年月日.日, 新しい順: true },
      ],
    };
    let 列の起点 = { genre: null, artist: null, album: null, 年: null, 月: null, 日: null };
    let 列の並び = { genre: [], artist: [], album: [], 年: [], 月: [], 日: [] };
    function 描き直す() { 環境.描き直した += 1; }
  `;
  // eslint-disable-next-line no-new-func
  const 動かす = new Function('環境', `${前置き}\n${切り出し5}\n`
    + ' return { 絞る, 選ぶ, sel: () => sel, タブを変える: (v) => { カラムタブ = v; },'
    + ' 並びを入れる: (k, a) => { 列の並び[k] = a; } };');
  return { 環境, ...動かす(環境) };
}

const 曲たち = [
  { path: 'p1', genre: 'G', artist: 'あ', album: 'A1', タグあり: true },
  { path: 'p2', genre: 'G', artist: 'い', album: 'A2', タグあり: true },
  { path: 'p3', genre: 'G', artist: 'う', album: 'A3', タグあり: true },
  { path: 'p4', genre: 'G', artist: 'え', album: 'A4', タグあり: true },
  { path: 'p5', genre: 'G', artist: 'AA', album: 'A5', タグあり: true },
  { path: 'p6', genre: 'G', artist: 'aa', album: 'A6', タグあり: true },
];
const 列の見え方 = ['あ', 'い', 'う', 'え', 'AA'];
const 台5 = () => { const t = 列を走らせる(曲たち); t.並びを入れる('artist', 列の見え方); return t; };

/* 5-1. ふつうのクリックは 1 つだけ */
{
  const { 選ぶ, 絞る, sel } = 台5();
  選ぶ(1, 'あ', {});
  確認4('ふつうのクリックは 1 つだけ', sel().artist.size === 1 && 絞る(2).length === 1);
}

/* 5-2. Ctrl で足せる */
{
  const { 選ぶ, 絞る, sel } = 台5();
  選ぶ(1, 'あ', {});
  選ぶ(1, 'う', { ctrlKey: true });
  確認4('★Ctrl+クリックで足せる', sel().artist.size === 2 && 絞る(2).length === 2, `${絞る(2).length} 曲`);
}

/* 5-3. ★個別に外せる（本人の希望の後半） */
{
  const { 選ぶ, 絞る, sel } = 台5();
  選ぶ(1, 'あ', {});
  選ぶ(1, 'い', { ctrlKey: true });
  選ぶ(1, 'う', { ctrlKey: true });
  選ぶ(1, 'い', { ctrlKey: true });            // もう一度押して外す
  const 残り = [...sel().artist].sort().join(',');
  確認4('★Ctrl+クリックで個別に外せる', 残り === 'あ,う', `残り: ${残り}`);
  確認4('外したぶんは絞り込みからも消える', 絞る(2).length === 2, `${絞る(2).length} 曲`);
}

/* 5-4. 全部外したら「すべて」に戻る（空にしない） */
{
  const { 選ぶ, 絞る, sel } = 台5();
  選ぶ(1, 'あ', {});
  選ぶ(1, 'あ', { ctrlKey: true });            // 最後の 1 つを外す
  確認4('★全部外したら「すべて」に戻る', sel().artist === null, '空集合のままだと一覧が 0 件になります');
  確認4('そのとき全曲が対象に戻る', 絞る(2).length === 曲たち.length, `${絞る(2).length} 曲`);
}

/* 5-5. Shift で範囲 */
{
  const { 選ぶ, sel } = 台5();
  選ぶ(1, 'あ', {});                            // 起点
  選ぶ(1, 'え', { shiftKey: true });            // あ〜え
  const 得 = [...sel().artist].sort().join(',');
  確認4('★Shift+クリックで範囲を選べる', sel().artist.size === 4, `選ばれたもの: ${得}`);
}

/* 5-6. 大文字小文字は同じものとして扱う（列のまとめ方と揃える） */
{
  const { 選ぶ, 絞る } = 台5();
  選ぶ(1, 'AA', {});
  確認4(
    '★大文字小文字が違っても同じものとして選ばれる',
    絞る(2).length === 2,
    `AA と aa で ${絞る(2).length} 曲（1 だと、まとめて出したのに片方しか出ないことになります）`,
  );
}

/* 5-7. 上の列を選び直したら、下の選択は外れる */
{
  const { 選ぶ, sel } = 台5();
  選ぶ(1, 'あ', {});
  選ぶ(2, 'A1', {});
  確認4('アルバムも選べる', sel().album !== null);
  選ぶ(1, 'い', {});                            // 上の列を選び直す
  確認4('★上の列を選び直すと、下の選択は外れる', sel().album === null, '実体の無い組み合わせが残ると 0 件の理由が分からなくなります');
}

/* ───────────────────────────────────────────────
   6. タブを切り替えても、前のタブの絞り込みが残るか（2026-08-29 本人の選択）

   ★これは「残す」と決めた仕様そのもの。緩んだら黙って曲が増える。
   ─────────────────────────────────────────────── */
console.log('\n■ タブの掛け合わせ');

const 日時 = (s) => new Date(s).getTime();
const 曲たち6 = [
  { path: 'q1', genre: 'Hardcore', artist: 'あ', album: 'A', タグあり: true, 更新日時: 日時('2015-07-13') },
  { path: 'q2', genre: 'Hardcore', artist: 'い', album: 'A', タグあり: true, 更新日時: 日時('2026-08-25') },
  { path: 'q3', genre: 'Punk',     artist: 'う', album: 'A', タグあり: true, 更新日時: 日時('2015-07-13') },
  { path: 'q4', genre: 'Punk',     artist: 'え', album: 'A', タグあり: true, 更新日時: 日時('2026-08-25') },
  { path: 'q5', genre: 'Punk',     artist: 'お', album: 'A', タグあり: true, 更新日時: 0 },   // 日付なし
];

{
  const { 絞る, 選ぶ, sel, タブを変える } = 列を走らせる(曲たち6);
  確認4('はじめは全部出る', 絞る(3).length === 5, `${絞る(3).length} 曲`);

  選ぶ(0, 'Hardcore', {});                       // タグタブ: ジャンル
  確認4('ジャンルで絞れる', 絞る(3).length === 2, `${絞る(3).length} 曲`);

  /*
   * ★2026-08-31、ここを作り直した（本人の指摘）。
   *
   *   > 操作が煩雑で自分で色々試して仕様を調べないと正しく動かすことができない
   *   > 何かの仕様を捨てないとシンプルで直感的な操作ができない
   *
   * 前は「タブを切り替えても前のタブの絞り込みが残る」（2026-08-29 本人の希望）。
   * タブが 2 つのころは掛け合わせに意味があったが、4 つになり、
   * 響きが暗黙に絞るようになってからは、**見えない絞り**の source になっていた。
   * ★掛け合わせは使っていないと本人に確認済み。
   */
  タブを変える('date');
  確認4(
    '★タブを切り替えたら、前のタブの絞り込みは効かない',
    絞る(3).length === 5,
    `${絞る(3).length} 曲（5 に戻らなければ、見えない絞りが残っています）`,
  );

  /* 候補も、いまのタブだけで出る */
  const 年の候補 = [...new Set(絞る(0)
    .map((t) => (t.更新日時 > 0 ? String(new Date(t.更新日時).getFullYear()) : null))
    .filter(Boolean))].sort();
  確認4('★候補も、隠れたタブに引きずられない', 年の候補.length >= 2, `候補: ${年の候補.join(',')}`);

  選ぶ(0, '2015', {});
  確認4('いまのタブでは、ちゃんと絞れる', 絞る(3).length === 2, `${絞る(3).length} 曲`);

  タブを変える('tag');
  確認4(
    '★戻ると、こちらのタブの選択が効く',
    絞る(3).length === 2,
    `${絞る(3).length} 曲（ジャンルの選択が残っているので 2 曲）`,
  );
  確認4(
    '★選択そのものは、どちらのタブにも残っている（消してはいない）',
    !!sel().genre && !!sel().年,
    '効かないだけで、覚えてはいます。戻れば効きます',
  );

  選ぶ(0, null);
  確認4(
    '★「すべて」で外せば、そのタブでは全部出る',
    絞る(3).length === 5 && !sel().genre,
    `${絞る(3).length} 曲`,
  );
}

/* 6-b. 日付の無い曲は、日付で絞ったら出ない */
{
  const { 絞る, 選ぶ, タブを変える } = 列を走らせる(曲たち6);
  タブを変える('date');
  選ぶ(0, '2026', {});
  const 出た = 絞る(3).map((t) => t.path).sort().join(',');
  確認4('★日付の無い曲は、日付で絞ったら出ない', 出た === 'q2,q4', `出たもの: ${出た}`);
}

/* ───────────────────────────────────────────────
   7. 同じアーティスト・同じアルバムを続けない（2026-08-29 本人の希望）

   ★測ってから入れた。広いところでは元から起きていない。
     全部 86,044 曲 … 直前と同じ演者 0.1%
     アルバム 3 枚 35 曲 … 32.5% → 1.6%（この直し）
   効くのは、3 カラムで数枚だけ選んだとき。
   ─────────────────────────────────────────────── */
console.log('\n■ 同じアーティスト・アルバムを続けない');

const 曲情報 = {
  a1: { artist: 'A', album: 'X' }, a2: { artist: 'A', album: 'X' },
  a3: { artist: 'A', album: 'Y' },
  b1: { artist: 'B', album: 'Z' }, b2: { artist: 'B', album: 'Z' },
};
const 手 = (直前) => ({ 直前: 曲情報[直前], 情報: (p) => 曲情報[p] ?? null });

{
  const 出 = {};
  for (let i = 0; i < 400; i += 1) {
    const p = 次を選ぶ(['a1', 'a2', 'a3', 'b1', 'b2'], {}, new Set(), Math.random, 手('a1'));
    出[p] = (出[p] ?? 0) + 1;
  }
  確認4(
    '★直前と同じアーティストは選ばれない',
    !出.a1 && !出.a2 && !出.a3,
    `出たもの: ${JSON.stringify(出)}`,
  );
  確認4('別のアーティストからは、どちらも出る', !!出.b1 && !!出.b2, JSON.stringify(出));
}

{
  // 同じアーティストしかいない → せめて別のアルバムにする
  const 出 = {};
  for (let i = 0; i < 400; i += 1) {
    const p = 次を選ぶ(['a1', 'a2', 'a3'], {}, new Set(), Math.random, 手('a1'));
    出[p] = (出[p] ?? 0) + 1;
  }
  確認4(
    '★同じ演者しかいないときは、せめて別のアルバムにする',
    !出.a1 && !出.a2 && 出.a3 === 400,
    `出たもの: ${JSON.stringify(出)}`,
  );
}

{
  // 1 枚しか選んでいない → 避けようがない。止まらずに返すこと
  const 出 = {};
  for (let i = 0; i < 400; i += 1) {
    const p = 次を選ぶ(['a1', 'a2'], {}, new Set(), Math.random, 手('a1'));
    出[p] = (出[p] ?? 0) + 1;
  }
  確認4(
    '★避けられないときは避けない（止まらない）',
    (出.a1 ?? 0) + (出.a2 ?? 0) === 400,
    `出たもの: ${JSON.stringify(出)}（ここで null を返すと、1 枚選んだとき何も流れません）`,
  );
}

{
  // 締め出していないか。巡を回して、全部いつかは出ること
  const 候補 = ['a1', 'a2', 'a3', 'b1', 'b2'];
  const 出た = new Set();
  let 済 = new Set(); let 前 = null;
  for (let i = 0; i < 300; i += 1) {
    if (巡が終わったか(候補, 済)) 済 = new Set();
    const p = 次を選ぶ(候補, {}, 済, Math.random, 前 ? 手(前) : null);
    済.add(p); 出た.add(p); 前 = p;
  }
  確認4('★どの曲も締め出さない', 出た.size === 候補.length, `出たのは ${出た.size} / ${候補.length} 曲`);
}

{
  // 手がかりを渡さなければ、今までどおり
  const 出 = {};
  for (let i = 0; i < 400; i += 1) { const p = 次を選ぶ(['a1', 'b1'], {}, new Set()); 出[p] = (出[p] ?? 0) + 1; }
  確認4('手がかりを渡さなければ、これまでどおり全部から選ぶ', !!出.a1 && !!出.b1, JSON.stringify(出));
}

{
  /*
   * ★画面の文が、古い決まりを喋っていないか（2026-08-31）。
   * 中身を直しても文が残っていると、読んだ人はそちらを信じる。
   * ここは「効いていないものを、効いているように見せない」ための見張り。
   */
  確認4(
    '★組む範囲の欄も、効いている選択だけを並べる',
    画面.includes('const 効く鍵 = new Set((カラムタブの列[カラムタブ] || []).map((c) => c.key));')
      && 画面.includes('.filter(([k, v]) => v && 効く鍵.has(k))'),
    '隠れたタブの選択まで書くと、曲数と食い違います',
  );
  確認4(
    '★「絞り込みは効いています」とは、もう言わない',
    !画面.includes('個選んだままです（絞り込みは効いています）'),
    '隠れたタブは効きません',
  );
}

console.log(失敗4 ? `\n★ ${失敗4} 件だめでした\n` : '\nすべて通りました\n');
process.exit(失敗4 ? 1 : 0);
